---
phase: 05-shared-stateful-services
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - infrastructure/opentofu/services/main.tf
  - infrastructure/opentofu/services/variables.tf
  - infrastructure/opentofu/services/outputs.tf
  - infrastructure/opentofu/services/providers.tf
  - infrastructure/opentofu/services/cloud-init.yaml.tftpl
  - scripts/services-platform.sh
autonomous: false
requirements:
  - SERV-02
  - SERV-03
  - SERV-04
must_haves:
  truths:
    - "services-01 is defined at 10.10.30.101 with bounded resources and Docker."
    - "The services lifecycle script provisions and manages the VM."
---

# Plan: 02 Services VM

## Must Haves

must_haves:
  truths:
    - Shared Services VM is defined via OpenTofu (services-01) with VMID 121 and IP 10.10.30.101.
    - Cloud-init installs Docker Engine.
    - `scripts/services-platform.sh` scaffolds the VM provisioning lifecycle.
  prohibitions: []

## Artifacts this phase produces

- File: `infrastructure/opentofu/services/main.tf`
- File: `infrastructure/opentofu/services/variables.tf`
- File: `infrastructure/opentofu/services/outputs.tf`
- File: `infrastructure/opentofu/services/providers.tf`
- File: `infrastructure/opentofu/services/cloud-init.yaml.tftpl`
- File: `scripts/services-platform.sh`
- CLI Flag: `scripts/services-platform.sh` subcommands (`preflight`, `apply`, `deploy`, `validate`, `destroy`)
- Proxmox VM: `services-01` (VMID 121)

## Tasks

<tasks>
  <task id="02-01" title="Create OpenTofu module for Services VM">
    <name>Task 1: Create OpenTofu module for Services VM</name>
    <files>infrastructure/opentofu/services/main.tf, infrastructure/opentofu/services/variables.tf, infrastructure/opentofu/services/outputs.tf, infrastructure/opentofu/services/providers.tf, infrastructure/opentofu/services/cloud-init.yaml.tftpl</files>
    <read_first>
      - infrastructure/opentofu/k3s/main.tf
      - infrastructure/opentofu/k3s/cloud-init.yaml.tftpl
    </read_first>
    <action>
      Create `infrastructure/opentofu/services/providers.tf` and `variables.tf` based on the k3s pattern.
      Create `infrastructure/opentofu/services/main.tf` using `proxmox_virtual_environment_vm` with VMID `121`, name `services-01`, tags `["opentofu", "services", "durable"]`.
      Set CPU cores to `4`, memory dedicated to `8192`, disk size `64`. Set IP to `10.10.30.101/24` with gateway `10.10.30.1`.
      Set startup order to `20`.
      Create `infrastructure/opentofu/services/cloud-init.yaml.tftpl` with `runcmd` to install Docker (`curl -fsSL https://get.docker.com | sh`), add user `ubuntu` to `docker` group, and enable the `docker` systemd service.
      Create `outputs.tf` for VM IP and ID.
    </action>
    <verify><automated>tofu -chdir=infrastructure/opentofu/services fmt -check &amp;&amp; tofu -chdir=infrastructure/opentofu/services validate</automated></verify>
    <acceptance_criteria>
      - `tofu -chdir=infrastructure/opentofu/services fmt -check` passes
      - `infrastructure/opentofu/services/main.tf` contains `proxmox_virtual_environment_vm "services"`
      - IP is exactly `10.10.30.101`
      - `cloud-init.yaml.tftpl` contains `https://get.docker.com`
    </acceptance_criteria>
    <done>The services VM module validates with the specified identity, resources, network, and Docker bootstrap.</done>
  </task>

  <task id="02-02" title="Create Services platform script">
    <name>Task 2: Create Services platform script</name>
    <files>scripts/services-platform.sh</files>
    <read_first>
      - scripts/k3s-platform.sh
    </read_first>
    <action>
      Create `scripts/services-platform.sh` with subcommands: `preflight`, `apply`, `deploy`, `validate`, `destroy`.
      Implement `apply`, `preflight`, and `destroy` mimicking the `k3s-platform.sh` OpenTofu lifecycle.
      Leave `deploy` and `validate` as placeholder functions that exit 0 (these will be implemented in the next wave).
      Make the script executable.
    </action>
    <verify><automated>bash -n scripts/services-platform.sh &amp;&amp; test -x scripts/services-platform.sh &amp;&amp; bash scripts/services-platform.sh 2&gt;&amp;1 | rg -q 'preflight.*apply.*deploy.*validate.*destroy'</automated></verify>
    <acceptance_criteria>
      - `scripts/services-platform.sh` is executable
      - Script contains subcommands `preflight`, `apply`, `deploy`, `validate`, `destroy`
      - Running `./scripts/services-platform.sh` outputs the usage string without error
    </acceptance_criteria>
    <done>The executable services lifecycle script exposes the required provisioning and service commands.</done>
  </task>
</tasks>

## Threat Model

<threat_model>
- **T-05-04**: Shared VM runs out of resources affecting all stateful services. Mitigated by sizing the VM correctly (4 vCPU, 8GB RAM).
- **T-05-05**: Unauthorized access to docker socket. Mitigated by only exposing Docker internally to the `ubuntu` user and running services via Compose rather than exposing the TCP daemon.
</threat_model>
