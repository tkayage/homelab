---
phase: 05-shared-stateful-services
plan: 04
type: execute
wave: 3
depends_on:
  - 05-03
files_modified:
  - gitops/apps/shared-services/kustomization.yaml
  - gitops/apps/shared-services/namespace.yaml
  - gitops/apps/shared-services/service-postgres.yaml
  - gitops/apps/shared-services/endpointslice-postgres.yaml
  - gitops/apps/shared-services/service-valkey.yaml
  - gitops/apps/shared-services/endpointslice-valkey.yaml
  - gitops/apps/shared-services/service-nats.yaml
  - gitops/apps/shared-services/endpointslice-nats.yaml
  - gitops/apps/shared-services/service-debezium.yaml
  - gitops/apps/shared-services/endpointslice-debezium.yaml
  - gitops/apps/shared-services/service-zitadel.yaml
  - gitops/apps/shared-services/endpointslice-zitadel.yaml
  - tests/test-shared-services.sh
autonomous: false
requirements:
  - SERV-05
  - SERV-06
must_haves:
  truths:
    - "Selectorless Kubernetes Services provide stable names for all shared services."
    - "Static and live validation cover service rendering and in-cluster connectivity."
---

# Plan: 04 Kubernetes Discovery

## Must Haves

must_haves:
  truths:
    - Kubernetes selectorless Services and EndpointSlices exist for `postgres`, `valkey`, `nats`, `debezium`, and `zitadel` in the `shared-services` namespace.
    - Postgres points to `10.10.30.100`.
    - Valkey, NATS, and Debezium point to `10.10.30.101`.
    - Zitadel points to `10.10.30.236`.
    - EndpointSlices have the `endpointslice.kubernetes.io/managed-by: homelab-gitops` label.
    - Test script validates connectivity from within the cluster.
  prohibitions: []

## Artifacts this phase produces

- File: `gitops/apps/shared-services/kustomization.yaml`
- File: `gitops/apps/shared-services/namespace.yaml`
- File: `gitops/apps/shared-services/service-postgres.yaml`
- File: `gitops/apps/shared-services/endpointslice-postgres.yaml`
- File: `gitops/apps/shared-services/service-valkey.yaml`
- File: `gitops/apps/shared-services/endpointslice-valkey.yaml`
- File: `gitops/apps/shared-services/service-nats.yaml`
- File: `gitops/apps/shared-services/endpointslice-nats.yaml`
- File: `gitops/apps/shared-services/service-debezium.yaml`
- File: `gitops/apps/shared-services/endpointslice-debezium.yaml`
- File: `gitops/apps/shared-services/service-zitadel.yaml`
- File: `gitops/apps/shared-services/endpointslice-zitadel.yaml`
- File: `tests/test-shared-services.sh`
- CLI Flag: `tests/test-shared-services.sh` subcommands (`static`, `live`)
- Namespace: `shared-services`
- Services: `postgres`, `valkey`, `nats`, `debezium`, `zitadel`

## Tasks

<tasks>
  <task id="04-01" title="Create GitOps discovery definitions">
    <name>Task 1: Create GitOps discovery definitions</name>
    <files>gitops/apps/shared-services/kustomization.yaml, gitops/apps/shared-services/namespace.yaml, gitops/apps/shared-services/service-postgres.yaml, gitops/apps/shared-services/endpointslice-postgres.yaml, gitops/apps/shared-services/service-valkey.yaml, gitops/apps/shared-services/endpointslice-valkey.yaml, gitops/apps/shared-services/service-nats.yaml, gitops/apps/shared-services/endpointslice-nats.yaml, gitops/apps/shared-services/service-debezium.yaml, gitops/apps/shared-services/endpointslice-debezium.yaml, gitops/apps/shared-services/service-zitadel.yaml, gitops/apps/shared-services/endpointslice-zitadel.yaml</files>
    <read_first>
      - .planning/phases/05-shared-stateful-services/05-RESEARCH.md
      - gitops/apps/gitops-smoke/kustomization.yaml
    </read_first>
    <action>
      Create a new application config in `gitops/apps/shared-services/`.
      Create `namespace.yaml` for `shared-services`.
      Create paired `service-*.yaml` and `endpointslice-*.yaml` for:
      - `postgres`: IP `10.10.30.100`, Port `5432`
      - `valkey`: IP `10.10.30.101`, Port `6379`
      - `nats`: IP `10.10.30.101`, Ports `4222`, `8222`
      - `debezium`: IP `10.10.30.101`, Port `8080`
      - `zitadel`: IP `10.10.30.236`, Ports `443`, `8080`
      Ensure all EndpointSlices use `endpointslice.kubernetes.io/managed-by: homelab-gitops` to prevent K8s auto-reconciliation issues.
      Ensure all EndpointSlices use the `kubernetes.io/service-name: <name>` label to link with their respective Service.
      Create `kustomization.yaml` assembling all these resources.
    </action>
    <verify><automated>kubectl kustomize gitops/apps/shared-services &gt;/dev/null</automated></verify>
    <acceptance_criteria>
      - `gitops/apps/shared-services/kustomization.yaml` exists and points to all service and endpointslice files.
      - `kubectl kustomize gitops/apps/shared-services` renders correctly without errors.
      - `endpointslice-postgres.yaml` targets `10.10.30.100`.
      - `endpointslice-valkey.yaml` targets `10.10.30.101`.
      - `endpointslice-zitadel.yaml` targets `10.10.30.236`.
    </acceptance_criteria>
    <done>The shared-services namespace, selectorless Services, and linked EndpointSlices render successfully.</done>
  </task>

  <task id="04-02" title="Create validation script for shared services">
    <name>Task 2: Create validation script for shared services</name>
    <files>tests/test-shared-services.sh</files>
    <read_first>
      - tests/test-k3s-foundation.sh
    </read_first>
    <action>
      Create `tests/test-shared-services.sh` using the bash boilerplate from `test-k3s-foundation.sh`.
      Add a `static` command that runs `tofu -chdir=infrastructure/opentofu/postgres fmt -check` and `tofu -chdir=infrastructure/opentofu/services fmt -check` and Kustomize validation.
      Add a `live` command that:
      1. Uses `kubectl run` or `kubectl apply -f` to launch a temporary Pod with PostgreSQL and Redis clients.
      2. Verifies connectivity to `postgres.shared-services.svc.cluster.local:5432`.
      3. Verifies connectivity to `valkey.shared-services.svc.cluster.local:6379` via ping.
      4. Verifies NATS HTTP endpoint `nats.shared-services.svc.cluster.local:8222/healthz`.
      5. Verifies Debezium `debezium.shared-services.svc.cluster.local:8080/q/health`.
      Make script executable.
    </action>
    <verify><automated>bash -n tests/test-shared-services.sh &amp;&amp; bash tests/test-shared-services.sh static</automated></verify>
    <acceptance_criteria>
      - `tests/test-shared-services.sh` is executable.
      - `tests/test-shared-services.sh static` runs without executing any real deployment.
      - The script includes `pg_isready` and `redis-cli ping` commands targeted at `.svc.cluster.local` names.
    </acceptance_criteria>
    <done>The executable test harness provides offline rendering checks and live in-cluster connectivity checks.</done>
  </task>
</tasks>

## Threat Model

<threat_model>
- **T-05-09**: Endpoints are modified or managed by the k8s endpoint controller, breaking cluster DNS for external services. Mitigated by `endpointslice.kubernetes.io/managed-by: homelab-gitops`.
- **T-05-10**: Apps cannot verify identity of external services due to missing names. Mitigated by `kubernetes.io/service-name` bindings maintaining static `.svc.cluster.local` resolution.
</threat_model>
