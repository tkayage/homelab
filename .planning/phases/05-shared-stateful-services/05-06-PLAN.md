---
phase: 05-shared-stateful-services
plan: 06
type: execute
wave: 5
depends_on:
  - "05-05"
files_modified:
  - scripts/postgres-platform.sh
  - .planning/phases/05-shared-stateful-services/05-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
gap_closure: true
requirements:
  - SERV-07
user_setup:
  - service: synology-nas
    why: "The repository has no NAS credentials or API path, so only the operator can authorize the workstation as an NFS client."
    dashboard_config:
      - task: "Grant 10.10.30.70 read/write NFS access to /volume1/homelab-backups"
        location: "NAS NFS shared-folder permissions for 10.10.40.2:/volume1/homelab-backups"
must_haves:
  truths:
    - "The workstation at 10.10.30.70 can mount 10.10.40.2:/volume1/homelab-backups read/write."
    - "A newly generated pg_dumpall from postgres-01 is written to the NAS, not retained only on the source LXC or workstation."
    - "The exact NAS-resident backup restores into the disposable PostgreSQL 17 container on services-01 and reconstructs the Debezium role."
    - "Canonical Phase 5 verification records SERV-07 complete only after the live NAS write and restore commands pass."
  artifacts:
    - path: "scripts/postgres-platform.sh"
      provides: "Workstation-mediated NAS backup and isolated restore verification commands"
      contains: "10.10.40.2:/volume1/homelab-backups"
    - path: ".planning/phases/05-shared-stateful-services/05-VERIFICATION.md"
      provides: "Canonical live evidence for the SERV-07 NAS write and restore"
      contains: "SERV-07"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Milestone requirement status after live verification"
      contains: "SERV-07"
  key_links:
    - from: "scripts/postgres-platform.sh backup"
      to: "10.10.40.2:/volume1/homelab-backups/postgres"
      via: "NFSv4 mount on workstation 10.10.30.70 followed by streamed pg_dumpall write"
      pattern: "mount.*nfs|pg_dumpall"
    - from: "scripts/postgres-platform.sh restore-test"
      to: "postgres:17 scratch container on services-01"
      via: "Consume the exact absolute NAS pathname emitted by the immediately preceding backup command, stream it over SSH, verify reconstructed role, then remove the container"
      pattern: "gunzip.*ssh_svc|docker exec"
    - from: ".planning/phases/05-shared-stateful-services/05-VERIFICATION.md"
      to: ".planning/REQUIREMENTS.md"
      via: "SERV-07 is marked complete only from recorded successful live commands"
      pattern: "SERV-07"
---

<objective>
Close the sole remaining Phase 5 verification gap by proving that a real PostgreSQL backup reaches the off-host NAS and that the same NAS-resident artifact remains restorable.

Purpose: Fulfill SERV-07 and the locked PostgreSQL backup decisions without pretending repository automation can change NAS export permissions. The operator first performs the one human-only authorization; all backup, restore, and evidence work after that is automated.
Output: A successful NAS write and isolated restore proof, plus canonical Phase 5 and milestone status updated from that live evidence.
</objective>

<execution_context>
@/home/tonny/.codex/gsd-core/workflows/execute-plan.md
@/home/tonny/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-shared-stateful-services/05-CONTEXT.md
@.planning/phases/05-shared-stateful-services/05-VERIFICATION.md
@.planning/phases/05-shared-stateful-services/05-05-SUMMARY.md
@scripts/postgres-platform.sh
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Authorize the workstation on the NAS NFS export</name>
  <files>scripts/postgres-platform.sh</files>
  <read_first>
    - scripts/postgres-platform.sh
    - .planning/phases/05-shared-stateful-services/05-05-SUMMARY.md
    - .planning/phases/05-shared-stateful-services/05-VERIFICATION.md
  </read_first>
  <action>
    On the NAS at 10.10.40.2, grant client 10.10.30.70 read/write NFS access to the existing shared folder exported as /volume1/homelab-backups. Preserve the export path and workstation-mediated design locked in Phase 5; do not move the backup onto Proxmox local storage, add SMB credentials, enable privileged LXC mounts, or claim that repository tooling changed the NAS permission. Limit the rule to the workstation client rather than authorizing the full subnet.
  </action>
  <verify>
    <automated>tmp=$(mktemp -d) &amp;&amp; sudo mount -t nfs -o hard,nfsvers=4,noatime 10.10.40.2:/volume1/homelab-backups "$tmp" &amp;&amp; probe="$tmp/.serv-07-write-${USER}-$$" &amp;&amp; printf 'SERV-07 NFS write probe\n' | sudo tee "$probe" &gt;/dev/null &amp;&amp; sudo test -s "$probe" &amp;&amp; sudo rm -f "$probe"; rc=$?; mountpoint -q "$tmp" &amp;&amp; sudo umount "$tmp"; rmdir "$tmp"; exit $rc</automated>
  </verify>
  <acceptance_criteria>
    - The exact export `10.10.40.2:/volume1/homelab-backups` mounts from workstation `10.10.30.70` with NFSv4.
    - A probe file can be created, read as non-empty, and removed on the mounted export.
    - NAS authorization is restricted to the workstation client and provides read/write access.
  </acceptance_criteria>
  <done>The executor's automated mount/write/remove probe exits 0; reply `granted` after changing the NAS permission so execution can continue.</done>
</task>

<task type="auto">
  <name>Task 2: Write a real backup to NAS, restore it, and close canonical evidence</name>
  <files>scripts/postgres-platform.sh, .planning/phases/05-shared-stateful-services/05-VERIFICATION.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - scripts/postgres-platform.sh
    - .planning/phases/05-shared-stateful-services/05-CONTEXT.md
    - .planning/phases/05-shared-stateful-services/05-VERIFICATION.md
    - .planning/phases/05-shared-stateful-services/05-05-SUMMARY.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
  </read_first>
  <action>
    Make the backup-to-restore interface deterministic before the live run. Keep `backup` as the producer, but require it to validate the new file as non-empty and gzip-valid while the NAS is mounted, then emit a stable final line `BACKUP_PATH=/mnt/pg-backup/postgres/pg_dumpall_*.sql.gz`. Change `restore-test` to require that exact absolute pathname as its sole argument: `restore-test <backup-path>`. After mounting the NAS, it must reject paths outside the canonical `$backup_mount/$backup_subdir` directory, reject names outside the `pg_dumpall_*.sql.gz` contract, require the exact file to be non-empty and gzip-valid, and restore that file rather than independently selecting the latest backup. Preserve the disposable `postgres:17` restore on services-01, Debezium LOGIN/REPLICATION assertion, and scratch-container cleanup.

    Run `bash scripts/postgres-platform.sh backup` from workstation 10.10.30.70, capture exactly one `BACKUP_PATH=` value from its output, and pass that same value to `bash scripts/postgres-platform.sh restore-test "$backup_path"`. Assert that the restore success output identifies the same pathname. A concurrent backup must not alter which artifact is restored. If cleanup or error handling prevents a trustworthy run, make only the smallest correction needed and rerun the paired commands from the beginning. Never restore into postgres-01 or another live database.

    Only after both live commands exit 0, replace the stale amended claims in `05-VERIFICATION.md` with dated evidence: exact commands, NAS export and backup pathname, non-empty/gzip validation, restore result, and final `status: passed` with 7/7 requirements live-verified. Mark SERV-07 complete in `REQUIREMENTS.md`, change Phase 5 to complete with 6/6 plans in `ROADMAP.md`, and remove the Phase 5 deferred-verification blocker from `STATE.md` while preserving the current Phase 6 position. Do not mark any unrelated Phase 6 requirement complete. If either live command fails, leave all completion statuses unchanged and record the failure in `05-06-SUMMARY.md` rather than fabricating closure.
  </action>
  <verify>
    <automated>backup_output=$(bash scripts/postgres-platform.sh backup) &amp;&amp; printf '%s\n' "$backup_output" &amp;&amp; backup_path=$(printf '%s\n' "$backup_output" | sed -n 's/^BACKUP_PATH=//p') &amp;&amp; test "$(printf '%s\n' "$backup_path" | wc -l)" -eq 1 &amp;&amp; [[ "$backup_path" == /mnt/pg-backup/postgres/pg_dumpall_*.sql.gz ]] &amp;&amp; restore_output=$(bash scripts/postgres-platform.sh restore-test "$backup_path") &amp;&amp; printf '%s\n' "$restore_output" &amp;&amp; printf '%s\n' "$restore_output" | grep -Fq "$backup_path" &amp;&amp; rg -n '^status: passed$|7/7 requirements live-verified|SERV-07' .planning/phases/05-shared-stateful-services/05-VERIFICATION.md .planning/REQUIREMENTS.md &amp;&amp; rg -n '6/6 plans complete|Phase 5: Shared Stateful Services.*completed' .planning/ROADMAP.md &amp;&amp; ! rg -n 'SERV-07.*(pending|blocked)|Phase 5 \(SERV-07\).*verification_deferred' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - A fresh, non-empty, gzip-valid `pg_dumpall_*.sql.gz` exists under the NAS export's `postgres/` directory.
    - `backup` and `restore-test` both exit 0 in the same execution after the NAS permission grant.
    - Restore verification uses a disposable PostgreSQL 17 container on services-01, reconstructs the Debezium role, and leaves no scratch container running.
    - `05-VERIFICATION.md` contains dated live evidence and reports `status: passed` and 7/7 requirements.
    - SERV-07, Phase 5 roadmap status, and deferred state are updated consistently; unrelated requirement statuses are untouched.
  </acceptance_criteria>
  <done>SERV-07 is backed by a real NAS-resident dump, a successful isolated restore, and consistent canonical verification/status artifacts.</done>
</task>

</tasks>

<threat_model>
## Security Profile

- OWASP ASVS level: 1
- Blocking threshold: high

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workstation to NAS NFS export | The workstation writes database backups across the LAN to durable off-host storage. |
| postgres-01 to workstation | PostgreSQL logical dump data crosses SSH from the source LXC. |
| Workstation to services-01 | The NAS backup crosses SSH into a disposable restore container for validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06-01 | Elevation of Privilege | NAS NFS client authorization | high | mitigate | Authorize only 10.10.30.70 for read/write access; do not grant the subnet or anonymous clients. Task 1 blocks until a real write/delete probe passes. |
| T-05-06-02 | Tampering | NAS-resident `pg_dumpall_*.sql.gz` | high | mitigate | Require a non-empty file, `gzip -t`, and a full restore from the same NAS path before accepting SERV-07. |
| T-05-06-03 | Information Disclosure | Logical database dump over LAN and at rest | medium | mitigate | Stream source and restore traffic through SSH, keep the export client-scoped, and avoid copying the dump into git or `.planning`. NAS at-rest encryption remains the NAS operator's control boundary. |
| T-05-06-04 | Denial of Service | Hard NFS mount and failed cleanup | medium | mitigate | Use the existing mount cleanup path, remove the scratch container, and fail without changing canonical completion state when backup or restore fails. |
| T-05-06-05 | Tampering | Production PostgreSQL during restore test | critical | mitigate | Restore only into the disposable PostgreSQL 17 container on services-01 and verify it is removed; never feed `pg_dumpall` into postgres-01. |
</threat_model>

<verification>
1. The human-only NAS permission is proven by an automated NFS mount/write/read/delete probe from 10.10.30.70.
2. `bash scripts/postgres-platform.sh backup` creates a fresh, non-empty, gzip-valid dump on `10.10.40.2:/volume1/homelab-backups/postgres` and emits its exact `BACKUP_PATH`.
3. `bash scripts/postgres-platform.sh restore-test "$backup_path"` restores that exact NAS artifact into the disposable PostgreSQL 17 container and exits 0 after verifying reconstructed state and reporting the same pathname.
4. Canonical verification, requirement, roadmap, and state files agree that SERV-07 and Phase 5 are complete, with no unrelated status changes.
</verification>

<success_criteria>
- SERV-07 has live evidence for both halves of its contract: off-host NAS storage and successful restore.
- No backup or restore is performed against production-local storage or a live PostgreSQL instance.
- Phase 5 reports 7/7 requirements live-verified and no longer appears in deferred verification state.
</success_criteria>

## Artifacts this phase produces

- A fresh `postgres/pg_dumpall_*.sql.gz` on `10.10.40.2:/volume1/homelab-backups` (runtime NAS artifact; never committed).
- `.planning/phases/05-shared-stateful-services/05-VERIFICATION.md` with dated live NAS-write and restore evidence.
- `.planning/phases/05-shared-stateful-services/05-06-SUMMARY.md` recording command results and the verified NAS backup pathname.
- Consistent SERV-07/Phase 5 completion state in `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md`.

<output>
Create `.planning/phases/05-shared-stateful-services/05-06-SUMMARY.md` when done.
</output>
