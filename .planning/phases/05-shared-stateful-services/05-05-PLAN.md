---
phase: 05-shared-stateful-services
plan: 05
type: execute
wave: 4
depends_on: ["05-01", "05-02", "05-03", "05-04"]
files_modified: []
autonomous: false
requirements:
  - SERV-01
  - SERV-02
  - SERV-03
  - SERV-04
  - SERV-05
  - SERV-06
  - SERV-07
must_haves:
  truths:
    - "postgres-01 (VMID 120, 10.10.30.100) exists on Proxmox and accepts connections on 5432 from 10.10.30.0/24."
    - "services-01 (VMID 121, 10.10.30.101) exists on Proxmox and runs Valkey, NATS JetStream, and Debezium via Compose with healthy containers."
    - "A pg_dumpall backup lands on the NFS share and restores successfully into a scratch database."
    - "A k3s workload resolves and reaches postgres, valkey, nats, debezium, and zitadel via *.shared-services.svc.cluster.local."
  prohibitions:
    - "No plaintext credentials committed to git; .env exists only on services-01."
---

<objective>
Actually provision and validate the shared stateful services whose IaC, scripts, and manifests were produced by plans 01-04 but never executed.

Amendment context: Phase 05 was originally verified as complete on 2026-07-07, but verification checked only that artifacts exist. No `tofu apply` was ever run (no state files in `infrastructure/opentofu/postgres/` or `infrastructure/opentofu/services/`), and 10.10.30.100/.101 are unreachable. Plans 01-04 lacked the `checkpoint:human-action` deployment task that Phase 02 used (`02-01-PLAN.md` Task 4). This plan closes that gap.
</objective>

<prerequisites>
- Proxmox provider credentials env file present (same as used by `scripts/k3s-platform.sh apply`).
- `TF_VAR_state_passphrase` exported (OpenTofu state encryption; both modules refuse `init`/`plan`/`state` without it).
- NFS export `10.10.40.2:/volume1/backup/postgres` reachable from 10.10.30.100 (flagged as Unknown 2 in 05-RESEARCH.md — confirm before Task 1's backup step).
- Working kubeconfig for the k3s cluster (kubectl currently fails auth: "server has asked for the client to provide credentials"). Re-fetch via `scripts/k3s-platform.sh` kubeconfig retrieval before Task 3.
</prerequisites>

<tasks>
<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provision, configure, and prove backup of postgres-01</name>
  <files>infrastructure/opentofu/postgres, scripts/postgres-platform.sh</files>
  <action>Run `scripts/postgres-platform.sh preflight` then `apply` to create LXC 120. Run `configure` to install PostgreSQL 17 with wal_level=logical, max_replication_slots=4, max_wal_senders=4, max_slot_wal_keep_size=4GB, pg_hba for 10.10.30.0/24, and the debezium replication role. Run `validate`. Then run `backup` and `restore-test` to satisfy SERV-07. Stop if VMID 120 or 10.10.30.100 is already occupied by an undeclared guest.</action>
  <verify><automated>bash scripts/postgres-platform.sh validate &amp;&amp; bash scripts/postgres-platform.sh backup &amp;&amp; bash scripts/postgres-platform.sh restore-test</automated></verify>
  <done>PostgreSQL answers pg_isready from the LAN, replication settings match the plan, and a fresh NFS backup restores into a scratch database.</done>
</task>
<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Provision services-01 and deploy the Compose stack</name>
  <files>infrastructure/opentofu/services, infrastructure/services, scripts/services-platform.sh</files>
  <action>Run `scripts/services-platform.sh preflight` then `apply` to create VM 121 with Docker via cloud-init. Create `/opt/homelab/services/.env` on the remote host with DEBEZIUM_DB_PASSWORD matching the debezium role from Task 1 (never commit it). Run `deploy` to push infrastructure/services/ and `docker compose up -d`, then `validate`. Stop if VMID 121 or 10.10.30.101 is already occupied by an undeclared guest.</action>
  <verify><automated>bash scripts/services-platform.sh validate</automated></verify>
  <done>Valkey answers PING, NATS /healthz and /jsz report JetStream with bounded storage, and Debezium is healthy with an active replication slot on postgres-01.</done>
</task>
<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Verify k3s discovery end to end</name>
  <files>gitops/apps/shared-services, tests/test-shared-services.sh</files>
  <action>Restore working kubectl access. Confirm Argo CD has synced gitops/apps/shared-services (namespace, selectorless Services, EndpointSlices). Run the live validation suite, which exercises cluster-DNS reachability of postgres, valkey, nats, debezium, and zitadel plus Zitadel OIDC discovery at https://zitadel.kayage.co.</action>
  <verify><automated>bash tests/test-shared-services.sh static &amp;&amp; bash tests/test-shared-services.sh live</automated></verify>
  <done>A workload inside the cluster reaches every shared service by its stable *.shared-services.svc.cluster.local name.</done>
</task>
</tasks>

<verification>All three platform validations pass live; `tofu plan` reports no drift for both modules; `tests/test-shared-services.sh live` exits 0.</verification>
<success_criteria>SERV-01 through SERV-07 are satisfied by running, reachable, backed-up services — not just by committed artifacts.</success_criteria>
<output>Create 05-05-SUMMARY.md when complete, then re-run phase verification to supersede the 2026-07-07 VERIFICATION.md.</output>
