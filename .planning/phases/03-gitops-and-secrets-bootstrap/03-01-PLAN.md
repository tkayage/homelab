---
phase: 03-gitops-and-secrets-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - gitops/platform/applicationset.yaml
  - gitops/apps/gitops-smoke/kustomization.yaml
  - gitops/apps/gitops-smoke/deployment.yaml
  - gitops/apps/gitops-smoke/secret.enc.yaml
  - infrastructure/kubernetes/argocd/cmp-plugin.yaml
  - infrastructure/kubernetes/argocd/repo-server-patch.yaml
  - scripts/gitops-platform.sh
  - tests/test-gitops-bootstrap.sh
  - docs/gitops-bootstrap.md
autonomous: true
requirements: [GITOPS-01, GITOPS-02, GITOPS-05, GITOPS-06]
must_haves:
  truths:
    - "Argo CD reconciles the private GitOps repository and discovers apps/* automatically."
    - "SOPS ciphertext decrypts only during reconciliation using a rebuild-safe external age key."
    - "A Git revert demonstrably restores a deliberately broken workload."
    - "Platform roots cannot prune and Application deletion does not cascade by default."
  artifacts:
    - path: scripts/gitops-platform.sh
      provides: Idempotent secret-safe bootstrap, status, rollback proof, and recovery workflow
    - path: infrastructure/kubernetes/argocd/cmp-plugin.yaml
      provides: SOPS/age Config Management Plugin contract
    - path: tests/test-gitops-bootstrap.sh
      provides: Static, live, and recovery acceptance gates
    - path: docs/gitops-bootstrap.md
      provides: Operator bootstrap, UI, diagnosis, rollback, and replacement runbook
  key_links:
    - from: gitops/platform/applicationset.yaml
      to: gitops/apps/*
      via: Git directory generator with automated sync and guarded deletion
    - from: gitops/apps/gitops-smoke/secret.enc.yaml
      to: infrastructure/kubernetes/argocd/cmp-plugin.yaml
      via: temporary-tree SOPS decryption using external age key
    - from: scripts/gitops-platform.sh
      to: tkayage/gitops-homelab
      via: credential-safe clone, commit, push, and root bootstrap
---

<objective>Bootstrap an authoritative, observable, rebuild-safe GitOps control plane and prove reconciliation and rollback.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Implement guarded GitOps and encrypted-secret manifests</name>
  <files>gitops/platform/applicationset.yaml, gitops/apps/gitops-smoke/kustomization.yaml, gitops/apps/gitops-smoke/deployment.yaml, gitops/apps/gitops-smoke/secret.enc.yaml, infrastructure/kubernetes/argocd/cmp-plugin.yaml, infrastructure/kubernetes/argocd/repo-server-patch.yaml</files>
  <action>Create the platform ApplicationSet and discovered smoke workload. Configure the platform root without prune/finalizers, generated applications with automated prune/self-heal and preserved resources, and a SOPS CMP sidecar that decrypts only in a temporary copy. Generate the age identity outside git and commit only ciphertext/public metadata.</action>
  <verify><automated>bash tests/test-gitops-bootstrap.sh static</automated></verify>
  <done>Manifests encode discovery, decryption, and deletion safety without plaintext credentials.</done>
</task>
<task type="auto">
  <name>Task 2: Build bootstrap automation, acceptance tests, and runbook</name>
  <files>.gitignore, scripts/gitops-platform.sh, tests/test-gitops-bootstrap.sh, docs/gitops-bootstrap.md</files>
  <action>Implement checksum-pinned tool installation, age key initialization, credential-safe publication to the private GitOps repository, pinned Argo CD installation, CMP patching, external credential bootstrap, root Application creation, status/UI helpers, and fail-closed tests. Document exact cluster replacement ordering and diagnostics.</action>
  <verify><automated>bash -n scripts/gitops-platform.sh &amp;&amp; bash -n tests/test-gitops-bootstrap.sh &amp;&amp; bash tests/test-gitops-bootstrap.sh static</automated></verify>
  <done>The workflow is idempotent, secret-safe, documented, and statically verified.</done>
</task>
<task type="auto">
  <name>Task 3: Bootstrap live reconciliation and prove rollback</name>
  <files>scripts/gitops-platform.sh, tests/test-gitops-bootstrap.sh</files>
  <action>Publish the initial GitOps repository, install Argo CD, bootstrap the external key and private-repository credential, apply the root Application, and wait for healthy reconciliation. Then push a controlled invalid smoke-workload revision, observe degradation, revert that exact Git commit, and prove automatic recovery. Finish with the recovery and live suites.</action>
  <verify><automated>bash scripts/gitops-platform.sh bootstrap &amp;&amp; bash scripts/gitops-platform.sh prove-rollback &amp;&amp; bash tests/test-gitops-bootstrap.sh live</automated></verify>
  <done>Actual Git reconciliation, secret decryption, observable failure, git revert, and recovery all pass.</done>
</task>
</tasks>

<verification>Static and live suites pass; Argo root and discovered application are Synced/Healthy; the decrypted test value is consumed; rollback recovery is recorded.</verification>
<success_criteria>GITOPS-01, GITOPS-02, GITOPS-05, and GITOPS-06 are satisfied by an operational GitOps and rebuild-safe secrets bootstrap.</success_criteria>
<output>Create 03-01-SUMMARY.md and 03-VERIFICATION.md when complete.</output>
