---
phase: 02-reproducible-k3s-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - infrastructure/opentofu/k3s/main.tf
  - infrastructure/opentofu/k3s/variables.tf
  - infrastructure/opentofu/k3s/outputs.tf
  - infrastructure/opentofu/k3s/cloud-init.yaml.tftpl
  - scripts/k3s-platform.sh
  - tests/test-k3s-foundation.sh
  - docs/k3s-foundation.md
autonomous: false
requirements: [INFRA-01, INFRA-02, INFRA-04]
must_haves:
  truths:
    - "VM 122 is reproducibly created from pinned version-controlled inputs with the Phase 1 allocation and network identity."
    - "Pinned k3s and bundled Traefik become healthy without manual guest configuration."
    - "Destroy/recreate restores cluster access while application persistence remains prohibited."
  artifacts:
    - path: infrastructure/opentofu/k3s/main.tf
      provides: OpenTofu Proxmox VM and cloud-init contract
    - path: scripts/k3s-platform.sh
      provides: Secret-safe provision, validate, destroy, and replace workflow
    - path: tests/test-k3s-foundation.sh
      provides: Static and live acceptance gates
  key_links:
    - from: infrastructure/inventory.json
      to: infrastructure/opentofu/k3s/main.tf
      via: exact k3s-01 identity and allocation assertions
    - from: scripts/k3s-platform.sh
      to: tests/test-k3s-foundation.sh
      via: live readiness and replacement commands
---

<objective>Provision and prove replacement of the disposable single-node k3s foundation.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Install and validate the pinned local toolchain</name>
  <files>.gitignore</files>
  <action>Install checksum-verified OpenTofu 1.12.1 and kubectl compatible with k3s v1.34; add comprehensive exclusions for state, plans, generated credentials, kubeconfig, downloaded images, and local runtime files. Derive a public key from the existing local SSH identity without exposing the private key.</action>
  <verify><automated>tofu version &amp;&amp; kubectl version --client</automated></verify>
  <done>Pinned tools work and generated sensitive/runtime artifacts cannot enter git.</done>
</task>
<task type="auto">
  <name>Task 2: Implement the OpenTofu and cloud-init contract</name>
  <files>infrastructure/opentofu/k3s/main.tf, infrastructure/opentofu/k3s/variables.tf, infrastructure/opentofu/k3s/outputs.tf, infrastructure/opentofu/k3s/cloud-init.yaml.tftpl</files>
  <action>Implement bpg/proxmox 0.111.x resources for Ubuntu 24.04, VMID 122, 4 vCPU, 8192 MiB, 64 GiB local-lvm disk, vmbr0 static 10.10.30.102/24 via 10.10.30.1, startup order 40/up delay 30, QEMU agent, and pinned k3s v1.34.9+k3s1. Read provider credentials only from the external env file. Use OpenTofu native local state encryption sourced from a non-git environment variable. Cloud-init installs QEMU agent and k3s, writes stable TLS SANs, and keeps kubeconfig 0600.</action>
  <verify><automated>tofu -chdir=infrastructure/opentofu/k3s fmt -check &amp;&amp; tofu -chdir=infrastructure/opentofu/k3s init -backend=false &amp;&amp; tofu -chdir=infrastructure/opentofu/k3s validate</automated></verify>
  <done>The configuration is valid, pinned, secret-free, and exactly matches the canonical inventory.</done>
</task>
<task type="auto">
  <name>Task 3: Add lifecycle automation, tests, and operator documentation</name>
  <files>scripts/k3s-platform.sh, tests/test-k3s-foundation.sh, docs/k3s-foundation.md</files>
  <action>Create fail-closed commands for preflight, apply, kubeconfig retrieval, readiness validation, destroy, and replace. Suppress credentials and raw authenticated responses. Tests must prove inventory alignment, pinning, git exclusions, state encryption, no application local-path persistence, and live node/system-pod/Traefik health. Document recovery and diagnostic paths.</action>
  <verify><automated>bash tests/test-k3s-foundation.sh static</automated></verify>
  <done>The lifecycle is automated, tested, and documented without secret leakage.</done>
</task>
<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Provision and exercise destructive replacement</name>
  <files>infrastructure/opentofu/k3s, scripts/k3s-platform.sh</files>
  <action>Run preflight and provision VM 122. Validate SSH, node Ready, system pods, bundled Traefik, expected LAN identity, and kubeconfig permissions. Then destroy and recreate VM 122 using identical inputs, retrieve a fresh kubeconfig, and rerun all checks. Stop before replacement only if another VM unexpectedly occupies VMID/IP or if the operator has placed undeclared data on the disposable guest.</action>
  <verify><automated>bash scripts/k3s-platform.sh replace &amp;&amp; bash tests/test-k3s-foundation.sh live</automated></verify>
  <done>An actual destroy/recreate exercise passes with no manual guest configuration.</done>
</task>
</tasks>

<verification>Static tests and the live post-replacement suite pass; OpenTofu reports no drift.</verification>
<success_criteria>INFRA-01, INFRA-02, and INFRA-04 are satisfied by a reproducible disposable k3s VM and tested recovery path.</success_criteria>
<output>Create 02-01-SUMMARY.md when complete.</output>
