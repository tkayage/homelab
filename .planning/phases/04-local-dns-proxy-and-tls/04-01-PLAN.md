---
phase: 04-local-dns-proxy-and-tls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infrastructure/edge/local-edge.json
  - scripts/local-edge.sh
  - tests/test-local-edge.sh
  - docs/local-edge.md
  - gitops/apps/edge-smoke/kustomization.yaml
  - gitops/apps/edge-smoke/deployment.yaml
  - gitops/apps/edge-smoke/service.yaml
  - gitops/apps/edge-smoke/ingress.yaml
autonomous: true
requirements: [EDGE-01, EDGE-02, EDGE-03, EDGE-04, EDGE-05]
must_haves:
  truths:
    - "Any committed *.app.kayage.co Ingress resolves locally to NPM and routes by Host through Traefik."
    - "NPM presents a valid Cloudflare DNS-01 wildcard certificate without public application DNS."
    - "Websocket, large-request, and forwarded-header behavior works through both proxies."
    - "Adding or removing an app hostname requires no Mikrotik, NPM, or certificate mutation."
  artifacts:
    - path: infrastructure/edge/local-edge.json
      provides: Non-secret desired wildcard DNS, proxy, TLS, and ownership contract
    - path: scripts/local-edge.sh
      provides: Idempotent scoped RouterOS/NPM/Cloudflare reconciliation
    - path: tests/test-local-edge.sh
      provides: Static and end-to-end protocol/TLS acceptance gates
    - path: gitops/apps/edge-smoke/ingress.yaml
      provides: GitOps-owned host-routing proof
  key_links:
    - from: infrastructure/edge/local-edge.json
      to: RouterOS and NPM
      via: labeled external API resources with stable ownership comments
    - from: gitops/apps/edge-smoke/ingress.yaml
      to: NPM wildcard proxy
      via: preserved Host header and Traefik ingressClass
    - from: /home/tonny/.config/homelab/cloudflare.env
      to: NPM certificate
      via: runtime-only DNS provider credentials
---

<objective>Deliver and prove a one-time wildcard LAN edge rail that gives every GitOps application valid-TLS local exposure.</objective>

<tasks>
<task type="auto">
  <name>Task 1: Implement scoped edge reconciliation and protocol smoke workload</name>
  <files>infrastructure/edge/local-edge.json, scripts/local-edge.sh, gitops/apps/edge-smoke/kustomization.yaml, gitops/apps/edge-smoke/deployment.yaml, gitops/apps/edge-smoke/service.yaml, gitops/apps/edge-smoke/ingress.yaml</files>
  <action>Define the exact wildcard rail and build fail-closed idempotent automation that manages only labeled RouterOS DNS and NPM certificate/proxy resources. Read all credentials externally, suppress credential-bearing payloads, refuse conflicts, and avoid public Cloudflare records. Add an auto-discovered GitOps echo workload with Traefik Ingress for request/header/websocket verification.</action>
  <verify><automated>bash tests/test-local-edge.sh static</automated></verify>
  <done>The external rail and in-cluster host contract are version-controlled, scoped, encrypted-secret-safe, and statically valid.</done>
</task>
<task type="auto">
  <name>Task 2: Reconcile live edge and prove all protocols</name>
  <files>scripts/local-edge.sh, tests/test-local-edge.sh</files>
  <action>Publish the edge-smoke app, reconcile RouterOS DNS, request/reuse the dedicated NPM Cloudflare DNS-01 certificate, and create the wildcard proxy. Verify direct DNS, trusted TLS hostname/chain, Host routing, websocket 101 upgrade, a multi-megabyte request, and forwarded client/proto/host headers.</action>
  <verify><automated>bash scripts/local-edge.sh apply &amp;&amp; bash tests/test-local-edge.sh live</automated></verify>
  <done>The actual LAN edge passes DNS, TLS, HTTP routing, websocket, large-body, and forwarding tests.</done>
</task>
<task type="auto">
  <name>Task 3: Prove zero-touch hostname lifecycle and document recovery</name>
  <files>tests/test-local-edge.sh, docs/local-edge.md</files>
  <action>Record the stable RouterOS/NPM/certificate resource identities, add and remove a temporary second Ingress hostname through Git only, and prove both lifecycle transitions without edge mutations. Document apply, status, diagnosis, renewal, conflict handling, and recovery.</action>
  <verify><automated>bash scripts/local-edge.sh prove-zero-touch &amp;&amp; bash tests/test-local-edge.sh live</automated></verify>
  <done>Git-only hostname add/remove is proven and the operator runbook covers recovery.</done>
</task>
</tasks>

<verification>All static/live tests pass and stable external resource IDs remain unchanged through the Git-only hostname lifecycle exercise.</verification>
<success_criteria>EDGE-01 through EDGE-05 are satisfied by a valid-TLS wildcard LAN rail and GitOps-owned per-app routing.</success_criteria>
<output>Create 04-01-SUMMARY.md and 04-VERIFICATION.md when complete.</output>
