---
phase: 08-end-to-end-validation-and-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-end-to-end-validation-and-operations/08-CONTEXT.md
  - .planning/phases/08-end-to-end-validation-and-operations/08-VERIFICATION.md
  - .planning/phases/08-end-to-end-validation-and-operations/08-01-SUMMARY.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - docs/runbooks/fintrack-deployment.md
  - .local/gitops-homelab/apps/fintrack/kustomization.yaml
  - .local/gitops-homelab/apps/fintrack/deployment.yaml
  - .local/gitops-homelab/apps/fintrack/pvc.yaml
  - .local/gitops-homelab/apps/fintrack/secret.enc.yaml
autonomous: true
requirements: [E2E-01, E2E-02, E2E-03, E2E-04, E2E-05]

must_haves:
  truths:
    - "fintrack is deployed as an outbound-only daemon with no Service, Ingress, health server, or public exposure marker."
    - "Runtime configuration and allowlist material are either SOPS-encrypted in GitOps or explicitly blocked as unavailable; no plaintext secrets are committed."
    - "The deployed workload uses the real /home/tonny/fintrack project image and a persistent /data volume."
    - "Verification records the selected-app deviations from URL/Postgres/Zitadel criteria instead of inventing fake integrations."
  artifacts:
    - path: ".local/gitops-homelab/apps/fintrack/deployment.yaml"
      provides: "GitOps Deployment for the daemon."
      contains: "ghcr.io/tkayage/fintrack"
    - path: "docs/runbooks/fintrack-deployment.md"
      provides: "Operator runbook for deployment, diagnosis, rollback, and recovery."
      contains: "outbound-only"
    - path: ".planning/phases/08-end-to-end-validation-and-operations/08-VERIFICATION.md"
      provides: "Live or blocked evidence for E2E closure."
      contains: "fintrack"
---

<objective>
Deploy `/home/tonny/fintrack` end to end through the homelab GitOps platform as a private outbound daemon, verify the real workload behavior that applies to this service, and document the operational runbook and selected-app deviations.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-end-to-end-validation-and-operations/08-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@/home/tonny/fintrack/README.md
@/home/tonny/fintrack/deploy/Dockerfile
@/home/tonny/fintrack/deploy/docker-compose.yml
@gitops/platform/applicationset.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preflight fintrack source, secrets, image publishing, and cluster access</name>
  <files>/home/tonny/fintrack, .local/gitops-homelab, .planning/phases/08-end-to-end-validation-and-operations/08-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/08-end-to-end-validation-and-operations/08-CONTEXT.md
    - /home/tonny/fintrack/deploy/Dockerfile
    - /home/tonny/fintrack/deploy/docker-compose.yml
    - /home/tonny/fintrack/.env.example
  </read_first>
  <action>
    Validate that the fintrack repository is clean enough to build, its Dockerfile builds the real daemon, Docker is available, cluster access works, and the GitOps worktree is usable. Check for runtime configuration sources and GHCR credentials without printing secret values. If required runtime secrets or image-push credentials are absent, stop at a clear blocker after writing verification evidence; do not fabricate values.
  </action>
  <verify>
    <automated>git -C /home/tonny/fintrack status --short --branch</automated>
    <automated>docker version</automated>
    <automated>KUBECONFIG=.local/kubeconfig-k3s-01 kubectl get nodes</automated>
  </verify>
  <acceptance_criteria>
    The execution path knows whether a real end-to-end deployment can proceed, and any blocker is specific to missing credentials/config rather than repository ambiguity.
  </acceptance_criteria>
  <done>Preflight completed with either deployable credentials or an explicit fail-closed blocker.</done>
</task>

<task type="auto">
  <name>Task 2: Build and publish the fintrack container image</name>
  <files>/home/tonny/fintrack/deploy/Dockerfile, .planning/phases/08-end-to-end-validation-and-operations/08-VERIFICATION.md</files>
  <read_first>
    - /home/tonny/fintrack/deploy/Dockerfile
    - .planning/phases/08-end-to-end-validation-and-operations/08-CONTEXT.md
  </read_first>
  <action>
    Build the real fintrack image from `/home/tonny/fintrack` and tag it as `ghcr.io/tkayage/fintrack:<short-sha-or-timestamp>`. Push it to GHCR when authenticated. Record the exact tag and digest or the exact blocker if GHCR publishing is unavailable.
  </action>
  <verify>
    <automated>docker build -f /home/tonny/fintrack/deploy/Dockerfile /home/tonny/fintrack</automated>
    <automated>docker push ghcr.io/tkayage/fintrack:&lt;tag&gt;</automated>
  </verify>
  <acceptance_criteria>
    A real image for the current fintrack source is available to the cluster, or image publishing is the only remaining blocker.
  </acceptance_criteria>
  <done>fintrack image built and published or publish blocker recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Register fintrack in GitOps without inbound exposure</name>
  <files>.local/gitops-homelab/apps/fintrack/kustomization.yaml, .local/gitops-homelab/apps/fintrack/deployment.yaml, .local/gitops-homelab/apps/fintrack/pvc.yaml, .local/gitops-homelab/apps/fintrack/secret.enc.yaml</files>
  <read_first>
    - gitops/platform/applicationset.yaml
    - .planning/phases/08-end-to-end-validation-and-operations/08-CONTEXT.md
  </read_first>
  <action>
    Create the `apps/fintrack` GitOps directory with a Deployment, PVC, and encrypted runtime Secret. The Deployment must reference the published image, mount `/data` and `/config/allowlist.json`, set required environment variables from the Secret, run as a non-root-compatible container, and include no Service or Ingress. Commit and push the GitOps change when the worktree remote is available.
  </action>
  <verify>
    <automated>kustomize build .local/gitops-homelab/apps/fintrack</automated>
    <automated>grep -R "kind: Ingress\\|kind: Service" .local/gitops-homelab/apps/fintrack && exit 1 || true</automated>
    <automated>git -C .local/gitops-homelab status --short</automated>
  </verify>
  <acceptance_criteria>
    GitOps contains a reviewable daemon deployment for fintrack with encrypted configuration and no inbound exposure.
  </acceptance_criteria>
  <done>fintrack registered in GitOps as an outbound-only daemon.</done>
</task>

<task type="auto">
  <name>Task 4: Reconcile, observe, rollback, and document operations</name>
  <files>.planning/phases/08-end-to-end-validation-and-operations/08-VERIFICATION.md, .planning/phases/08-end-to-end-validation-and-operations/08-01-SUMMARY.md, docs/runbooks/fintrack-deployment.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
  </read_first>
  <action>
    Verify Argo CD discovers and reconciles fintrack, confirm the pod reaches a meaningful live state or captures the exact startup failure, prove a failed deployment can be observed and recovered via GitOps rollback, and write the operator runbook. Record MS-01 restart recovery evidence if live reboot is appropriate; otherwise document the exact manual exercise and do not overstate it.
  </action>
  <verify>
    <automated>KUBECONFIG=.local/kubeconfig-k3s-01 kubectl -n argocd get applications</automated>
    <automated>KUBECONFIG=.local/kubeconfig-k3s-01 kubectl -n fintrack rollout status deployment/fintrack --timeout=180s</automated>
    <automated>KUBECONFIG=.local/kubeconfig-k3s-01 kubectl -n fintrack logs deployment/fintrack --tail=100</automated>
    <automated>node /home/tonny/.codex/gsd-core/bin/gsd-tools.cjs query init.verify-work 8</automated>
  </verify>
  <acceptance_criteria>
    Phase 8 has live evidence for the fintrack deployment path, explicit notes for non-applicable web criteria, rollback/recovery evidence or exact manual deferrals, and a usable runbook.
  </acceptance_criteria>
  <done>Phase 8 is verified complete for fintrack or blocked only by named external credentials/reboot decisions.</done>
</task>

</tasks>

<verification>
- Fintrack source tests/build checks pass enough to produce the Docker image.
- The GHCR image tag used by GitOps is recorded.
- `kustomize build .local/gitops-homelab/apps/fintrack` passes.
- No `Service`, `Ingress`, or public exposure marker exists under `apps/fintrack`.
- Kubernetes rollout/log evidence is captured when credentials are present.
- `08-VERIFICATION.md` documents E2E-01 through E2E-05 with selected-app deviations.
</verification>

<success_criteria>
The real fintrack daemon is either deployed and observed through the homelab GitOps platform, or the only remaining blockers are explicit missing external credentials/configuration that cannot be invented safely. The platform runbook explains how to operate, diagnose, roll back, and recover this deployment.
</success_criteria>
