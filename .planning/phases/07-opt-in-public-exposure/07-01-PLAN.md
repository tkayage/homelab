---
phase: 07-opt-in-public-exposure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scaffold/cmd/scaffold/main.go
  - scaffold/internal/scaffolder/scaffolder.go
  - scaffold/internal/scaffolder/scaffolder_test.go
  - scaffold/internal/manifests/manifests.go
  - scaffold/internal/manifests/manifests_test.go
  - scaffold/internal/manifests/testdata/apps-slug.golden.txt
  - scaffold/internal/templates/files/gitops/ingress.yaml.tmpl
  - scripts/public-edge.sh
  - tests/test-public-edge.sh
  - infrastructure/edge/public-edge.json
  - .planning/phases/07-opt-in-public-exposure/07-VERIFICATION.md
  - .planning/phases/07-opt-in-public-exposure/07-01-SUMMARY.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [PUBLIC-01, PUBLIC-02, PUBLIC-03, PUBLIC-04]

must_haves:
  truths:
    - "Generated apps are LAN-only by default and contain no public Cloudflare annotation/record marker unless explicitly requested."
    - "Public opt-in is represented in GitOps app manifests and is visible in code review."
    - "Public Cloudflare records are per-app, never a wildcard for *.app.kayage.co."
    - "Verification can prove admin surfaces are not exposed, with live checks when credentials/network are available and static fail-closed checks otherwise."
  artifacts:
    - path: "scaffold/internal/templates/files/gitops/ingress.yaml.tmpl"
      provides: "Default-deny Ingress template with conditional public metadata."
      contains: "external-dns.alpha.kubernetes.io"
    - path: "scripts/public-edge.sh"
      provides: "Idempotent public exposure preflight/status/proof commands."
      contains: "assert_no_public_wildcard"
    - path: "tests/test-public-edge.sh"
      provides: "Static validation for the public exposure contract."
      contains: "admin"
---

<objective>
Implement Phase 7's default-deny public exposure contract: generated apps remain LAN-only by default, an explicit public flag adds review-visible GitOps metadata for public DNS, public records are per-app only, and operators get script/test commands to verify public reachability and management-surface non-exposure.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-opt-in-public-exposure/07-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@scripts/local-edge.sh
@infrastructure/edge/local-edge.json
@scaffold/internal/templates/files/gitops/ingress.yaml.tmpl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add default-deny public flag to generated GitOps manifests</name>
  <files>scaffold/cmd/scaffold/main.go, scaffold/internal/scaffolder/scaffolder.go, scaffold/internal/manifests/manifests.go, scaffold/internal/templates/files/gitops/ingress.yaml.tmpl, scaffold/internal/manifests/manifests_test.go, scaffold/internal/manifests/testdata/apps-slug.golden.txt, scaffold/internal/scaffolder/scaffolder_test.go</files>
  <read_first>
    - .planning/phases/07-opt-in-public-exposure/07-CONTEXT.md
    - scaffold/internal/manifests/manifests.go
    - scaffold/internal/templates/files/gitops/ingress.yaml.tmpl
    - scaffold/internal/manifests/manifests_test.go
    - scaffold/internal/scaffolder/scaffolder.go
    - scaffold/cmd/scaffold/main.go
  </read_first>
  <action>
    Add a `Public` boolean to the scaffolder/manifests data path and expose it as a CLI flag named `--public`.
    The default must be false. When false, rendered Ingress has no public DNS annotation/marker. When true, the
    rendered Ingress includes a review-visible annotation set for public exposure, including a per-host target
    marker for the AWS public edge and an owner marker. Do not create a wildcard public record. Update tests and
    the kustomize golden so both default-deny and opt-in behavior are covered.
  </action>
  <verify>
    <automated>go test -C scaffold ./...</automated>
  </verify>
  <acceptance_criteria>
    Default render has no Cloudflare/public annotation. Public render includes public metadata for exactly
    `<slug>.app.kayage.co`. Existing scaffold verification still passes.
  </acceptance_criteria>
  <done>Generated app manifests are default-deny and support explicit public opt-in.</done>
</task>

<task type="auto">
  <name>Task 2: Add public edge contract, operator script, and static verification</name>
  <files>infrastructure/edge/public-edge.json, scripts/public-edge.sh, tests/test-public-edge.sh</files>
  <read_first>
    - scripts/local-edge.sh
    - infrastructure/edge/local-edge.json
    - tests/test-local-edge.sh
    - .planning/phases/07-opt-in-public-exposure/07-CONTEXT.md
  </read_first>
  <action>
    Add `infrastructure/edge/public-edge.json` describing the public edge contract: domain suffix, owner marker,
    public target host/IP placeholder, denylisted management hosts/ports, and proof hostname. Add
    `scripts/public-edge.sh` with subcommands `preflight`, `assert-default-deny`, `status`, `prove-enable`,
    `prove-disable`, and `scan-admin`. Reuse the style and credential-loading conventions from local-edge.sh.
    The script must fail if a public wildcard exists for `*.app.kayage.co`, operate only on platform-owned
    per-host records, and never print credentials. Add `tests/test-public-edge.sh static` to validate JSON shape,
    script syntax, no credential signatures, denylist presence, and no wildcard creation logic.
  </action>
  <verify>
    <automated>bash tests/test-public-edge.sh static</automated>
  </verify>
  <acceptance_criteria>
    Static tests prove the public edge contract is default-deny, per-host only, credential-clean, and includes admin
    surface denylist checks. Live commands are available for operator proof when public edge credentials/network are ready.
  </acceptance_criteria>
  <done>Public edge automation and static proof harness exist.</done>
</task>

<task type="auto">
  <name>Task 3: Verify and close Phase 7</name>
  <files>.planning/phases/07-opt-in-public-exposure/07-VERIFICATION.md, .planning/phases/07-opt-in-public-exposure/07-01-SUMMARY.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
  </read_first>
  <action>
    Run the full automated verification set: `go test -C scaffold ./...`, `bash scripts/scaffold-verify.sh all`,
    `bash tests/test-public-edge.sh static`, and any safe live public-edge preflight/status checks available without
    mutating external state. Write `07-VERIFICATION.md`, `07-01-SUMMARY.md`, and update REQUIREMENTS/ROADMAP/STATE.
    If the AWS Mikrotik/CHR public path is not live-testable from this machine, mark only that external proof as
    deferred to Phase 8 or manual operator verification; do not mark implementation gaps for static contract checks.
  </action>
  <verify>
    <automated>go test -C scaffold ./...</automated>
    <automated>bash scripts/scaffold-verify.sh all</automated>
    <automated>bash tests/test-public-edge.sh static</automated>
    <automated>node /home/tonny/.codex/gsd-core/bin/gsd-tools.cjs query init.verify-work 7</automated>
  </verify>
  <acceptance_criteria>
    PUBLIC-01 through PUBLIC-04 are either verified by static/live evidence or have a clearly scoped external live
    proof note. Phase 7 has a summary and verification artifact.
  </acceptance_criteria>
  <done>Phase 7 is verified complete or explicitly blocked only by unavailable external public-edge live proof.</done>
</task>

</tasks>

<verification>
- `go test -C scaffold ./...` passes.
- `bash scripts/scaffold-verify.sh all` passes.
- `bash tests/test-public-edge.sh static` passes.
- `07-VERIFICATION.md` records default-deny, public opt-in, disable path, and admin-surface scan evidence.
</verification>

<success_criteria>
An app is LAN-only unless `--public`/public metadata is explicitly enabled; public DNS is per-host only; operators have repeatable commands to prove enable, disable, and management-surface non-exposure.
</success_criteria>
