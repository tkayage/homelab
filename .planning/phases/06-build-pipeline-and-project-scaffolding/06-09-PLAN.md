---
phase: 06-build-pipeline-and-project-scaffolding
plan: 09
type: execute
wave: 7
depends_on: ["06-08"]
gap_closure: true
files_modified:
  - scaffold/cmd/scaffold/main.go
  - scaffold/internal/scaffolder/scaffolder.go
  - scaffold/internal/scaffolder/scaffolder_test.go
  - scaffold/internal/gitops/gitops.go
  - scaffold/internal/gitops/gitops_test.go
  - scaffold/internal/manifests/manifests.go
  - scaffold/internal/manifests/manifests_test.go
  - scaffold/internal/templates/templates.go
  - scaffold/internal/templates/templates_test.go
  - scaffold/internal/templates/files/workflow.deploy.yml.tmpl
  - scaffold/internal/templates/testdata/workflow.golden
  - scaffold/internal/templates/workflow_test.go
  - scripts/scaffold-verify.sh
  - .planning/phases/06-build-pipeline-and-project-scaffolding/06-VERIFICATION.md
  - .planning/phases/06-build-pipeline-and-project-scaffolding/06-09-SUMMARY.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [SCAF-04, GITOPS-04]
closes_gaps:
  - SCAF-04-PULL-SECRET-TOKEN
  - SCAF-04-PLAINTEXT-SECRET-LIFETIME
  - GITOPS-04-PUSH-RETRY-FALSE-SUCCESS
user_setup: []

must_haves:
  truths:
    - "A real non-dry-run publish cannot encrypt or commit a GHCR pull secret with an empty password."
    - "The GHCR pull token has an operator-safe input path that avoids process-list-visible CLI secrets; hidden --pull-password remains only an offline dummy/test seam."
    - "Plaintext pull-secret.yaml is written with restrictive mode and removed on every publish return path, including SOPS encryption failures."
    - "The generated GitHub Actions GitOps push retry loop exits nonzero after all retries fail."
  artifacts:
    - path: "scaffold/internal/scaffolder/scaffolder.go"
      provides: "Fail-closed credential validation and operator-safe token loading before GitOps publish."
      contains: "pull token"
    - path: "scaffold/internal/gitops/gitops.go"
      provides: "Plaintext pull-secret cleanup on success and failure."
      contains: "defer"
    - path: "scaffold/internal/templates/files/workflow.deploy.yml.tmpl"
      provides: "Retry loop with explicit exhausted-retry failure."
      contains: "pushed=0"
  key_links:
    - from: "06-VERIFICATION.md"
      to: "06-09-PLAN.md"
      via: "blocking gap IDs become executable remediation tasks"
      pattern: "SCAF-04-PULL-SECRET-TOKEN"
---

<objective>
Close the three Phase 6 verification blockers without broadening the phase scope: make private GHCR pull
credential handling production-safe, make the plaintext secret lifecycle fail-closed, and make the generated
GitHub Actions GitOps retry loop report exhausted push retries as a real failure.

Output: code/tests that prove the three blocker IDs in `06-VERIFICATION.md` are fixed, plus an updated
verification artifact showing Phase 6 can transition.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-build-pipeline-and-project-scaffolding/06-VERIFICATION.md
@.planning/phases/06-build-pipeline-and-project-scaffolding/06-REVIEW.md
@.planning/phases/06-build-pipeline-and-project-scaffolding/06-08-SUMMARY.md
@scaffold/cmd/scaffold/main.go
@scaffold/internal/scaffolder/scaffolder.go
@scaffold/internal/gitops/gitops.go
@scaffold/internal/manifests/manifests.go
@scaffold/internal/templates/templates.go
@scaffold/internal/templates/files/workflow.deploy.yml.tmpl
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Lock in failing tests for the three verification blockers</name>
  <files>
    scaffold/internal/scaffolder/scaffolder_test.go,
    scaffold/internal/gitops/gitops_test.go,
    scaffold/internal/manifests/manifests_test.go,
    scaffold/internal/templates/templates_test.go,
    scaffold/internal/templates/workflow_test.go
  </files>
  <read_first>
    - .planning/phases/06-build-pipeline-and-project-scaffolding/06-VERIFICATION.md (Blocking Gaps 1-3)
    - .planning/phases/06-build-pipeline-and-project-scaffolding/06-REVIEW.md (CR-01, CR-02, CR-03)
    - scaffold/internal/scaffolder/scaffolder_test.go (existing dry-run and publish-path seams)
    - scaffold/internal/gitops/gitops_test.go (existing Publish/SOPS behavior)
    - scaffold/internal/templates/workflow_test.go (existing workflow structural invariants)
  </read_first>
  <action>
    Add focused regression tests before implementation:
    (1) a non-dry-run publish path with no operator pull token fails before rendering/publishing a secret;
    (2) the supported real-token path reads from an environment variable or token file, while `--pull-password`
    remains documented and exercised only as a dummy/offline test seam;
    (3) `pull-secret.yaml` is rendered with mode 0600 and is removed when SOPS/encryption fails;
    (4) the workflow template/golden contains a `pushed` flag and an explicit `exit 1` branch after retry exhaustion.

    Keep tests deterministic. Use temp dirs, dummy tokens, and existing package test helpers. Do not require a
    live GitHub repo, real GHCR token, or network access.
  </action>
  <verify>
    <automated>go test -C scaffold ./...</automated>
  </verify>
  <acceptance_criteria>
    The new tests fail against the pre-fix behavior for each blocker and pass after Tasks 2-3. They identify the
    required production behavior without relying on live infrastructure.
  </acceptance_criteria>
  <done>Regression tests cover empty-token refusal, safe token input, restrictive secret mode/cleanup, and retry-loop exhausted failure.</done>
</task>

<task type="auto">
  <name>Task 2: Make GHCR pull secret credential handling and plaintext cleanup fail-closed</name>
  <files>
    scaffold/cmd/scaffold/main.go,
    scaffold/internal/scaffolder/scaffolder.go,
    scaffold/internal/gitops/gitops.go,
    scaffold/internal/manifests/manifests.go,
    scaffold/internal/templates/templates.go
  </files>
  <read_first>
    - scaffold/internal/scaffolder/scaffolder.go (Options.PullPassword, Run, publishGitops)
    - scaffold/cmd/scaffold/main.go (hidden --pull-password offline seam)
    - scaffold/internal/manifests/manifests.go (pull-secret render path)
    - scaffold/internal/templates/templates.go (RenderToFile mode)
    - scaffold/internal/gitops/gitops.go (Publish, encryptPullSecretWith, syncWorktree)
  </read_first>
  <action>
    Implement the smallest production-safe credential contract:
    - Add an operator-safe pull-token source that does not place the real token in argv. Prefer a token file flag
      such as `--pull-token-file` and/or an environment variable such as `GHCR_PULL_TOKEN`; document the precedence
      in flag help/report text.
    - Keep hidden `--pull-password` only for dummy/offline tests. Reject real non-dummy use or make it clearly
      subordinate to env/file loading so normal operators never pass secrets through process listings.
    - Fail before GitOps publish when the run is not dry-run and the resolved pull token is empty. The error should
      name the operator-safe mechanism and should not print the token.
    - Render `pull-secret.yaml` with mode 0600, not the shared 0644 non-secret path. Add a small mode-aware template
      helper if that best fits the current package shape.
    - In `gitops.Publish`, remove `apps/<slug>/pull-secret.yaml` on every return path after render, including SOPS
      failures. Cleanup must be best-effort and must not hide the original encryption/publish error.
    - Where easy and scoped, clean stale untracked `pull-secret.yaml` in the target app dir before rendering so a
      previous failed run cannot survive into a later publish.
  </action>
  <verify>
    <automated>go test -C scaffold ./...</automated>
    <automated>bash scripts/scaffold-verify.sh all</automated>
  </verify>
  <acceptance_criteria>
    A normal non-dry-run cannot produce a SOPS-encrypted pull secret with an empty password. Real tokens can be
    supplied without argv exposure. Plaintext pull-secret files are restrictive while present and are gone after
    success or failure.
  </acceptance_criteria>
  <done>SCAF-04-PULL-SECRET-TOKEN and SCAF-04-PLAINTEXT-SECRET-LIFETIME are fixed with tests and offline verification.</done>
</task>

<task type="auto">
  <name>Task 3: Make the generated GitOps push retry loop fail on exhausted retries</name>
  <files>
    scaffold/internal/templates/files/workflow.deploy.yml.tmpl,
    scaffold/internal/templates/testdata/workflow.golden,
    scaffold/internal/templates/workflow_test.go
  </files>
  <read_first>
    - scaffold/internal/templates/files/workflow.deploy.yml.tmpl (git pull/rebase + push retry loop)
    - scaffold/internal/templates/workflow_test.go (golden and structural tests)
    - .planning/phases/06-build-pipeline-and-project-scaffolding/06-REVIEW.md (CR-03)
  </read_first>
  <action>
    Replace the current `for i in 1 2 3; do git pull ... && git push ... && break; sleep ...; done`
    structure with an explicit success flag:

    ```bash
    pushed=0
    for i in 1 2 3; do
      if git pull --rebase origin main && git push origin main; then
        pushed=1
        break
      fi
      sleep $((RANDOM % 5 + 2))
    done
    if [ "$pushed" -ne 1 ]; then
      echo "::error::gitops push failed after 3 retries" >&2
      exit 1
    fi
    ```

    Regenerate/update the workflow golden and strengthen `TestWorkflowStructuralInvariants` so future edits must
    retain the success flag and exhausted-retry failure branch.
  </action>
  <verify>
    <automated>go test -C scaffold ./...</automated>
    <automated>bash --noprofile --norc -e -o pipefail -c 'pushed=0; for i in 1 2 3; do if false && false; then pushed=1; break; fi; sleep 0; done; if [ "$pushed" -ne 1 ]; then exit 1; fi'</automated>
    <automated>bash scripts/scaffold-verify.sh all</automated>
  </verify>
  <acceptance_criteria>
    The generated workflow cannot finish green when every GitOps pull/push retry fails. The golden file and a
    structural test enforce the failure branch.
  </acceptance_criteria>
  <done>GITOPS-04-PUSH-RETRY-FALSE-SUCCESS is fixed with template, golden, and structural-test coverage.</done>
</task>

<task type="auto">
  <name>Task 4: Re-verify and close Phase 6 gap state</name>
  <files>
    .planning/phases/06-build-pipeline-and-project-scaffolding/06-VERIFICATION.md,
    .planning/phases/06-build-pipeline-and-project-scaffolding/06-09-SUMMARY.md,
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md,
    .planning/STATE.md
  </files>
  <read_first>
    - .planning/phases/06-build-pipeline-and-project-scaffolding/06-VERIFICATION.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
  </read_first>
  <action>
    Run the full verification set after code fixes:
    - `go test -C scaffold ./...`
    - `bash scripts/scaffold-verify.sh all`
    - targeted retry-loop reproduction that now exits nonzero on all failures
    - targeted filesystem proof from tests or temp-run evidence that plaintext pull-secret mode is 0600 and cleanup
      happens on encryption failure

    Update `06-VERIFICATION.md` from `gaps_found` to passed only if the evidence proves all three blocker IDs are
    closed. Write `06-09-SUMMARY.md` with changed files, tests, and residual deferrals. Then update Phase 6 status
    in REQUIREMENTS/ROADMAP/STATE according to the existing GSD conventions.
  </action>
  <verify>
    <automated>go test -C scaffold ./...</automated>
    <automated>bash scripts/scaffold-verify.sh all</automated>
    <automated>node /home/tonny/.codex/gsd-core/scripts/gsd-tools.cjs query init.verify-work 6</automated>
  </verify>
  <acceptance_criteria>
    `init.verify-work 6` reports Phase 6 complete or ready to transition. `06-VERIFICATION.md` no longer lists
    the three blocker gaps. Residual live push->GHCR->Argo evidence remains explicitly deferred to Phase 8 only.
  </acceptance_criteria>
  <done>Phase 6 gap verification is closed and the workflow state points to the next phase/action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell -> scaffolder | real GHCR pull token enters through env/file, not argv |
| scaffolder -> gitops worktree | plaintext dockerconfigjson exists only transiently and with restrictive mode |
| gitops worktree -> SOPS ciphertext | only encrypted pull-secret.enc.yaml is staged/committed |
| GitHub Actions runner -> gitops-homelab | retry loop must fail the workflow if no GitOps bump is pushed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-20 | Information Disclosure | GHCR pull token argv exposure | high | mitigate | Real token source is env/file; hidden pull-password remains dummy/offline-only |
| T-06-21 | Denial of Service | empty private-image pull secret | high | mitigate | Non-dry-run publish fails before render/publish when token is empty |
| T-06-22 | Information Disclosure | plaintext pull-secret.yaml | high | mitigate | Render mode 0600 and defer cleanup on every publish return path |
| T-06-23 | Tampering | GitOps image bump push | high | mitigate | Workflow exits 1 after all pull/push retries fail |
</threat_model>

<verification>
- `go test -C scaffold ./...` passes.
- `bash scripts/scaffold-verify.sh all` passes.
- Non-dry-run publish without a resolved GHCR pull token fails before writing/publishing a secret.
- Real pull token input is available through env/file and does not require argv exposure.
- Plaintext `pull-secret.yaml` is mode 0600 while present and removed after success or SOPS failure.
- Generated workflow retry branch has `pushed=0`, sets success only after `git push`, and exits 1 after exhausted retries.
- `06-VERIFICATION.md` is updated from `gaps_found` after all three blocker IDs are fixed.
</verification>

<success_criteria>
Phase 6's production scaffolder contract is safe enough for Phase 8 to depend on: private GHCR pulls have a
real fail-closed token path, plaintext credentials do not persist in the GitOps worktree, and green GitHub
Actions deploy runs imply the GitOps image bump was actually pushed.
</success_criteria>

## Artifacts this plan produces
- Code fixes for `SCAF-04-PULL-SECRET-TOKEN`, `SCAF-04-PLAINTEXT-SECRET-LIFETIME`, and `GITOPS-04-PUSH-RETRY-FALSE-SUCCESS`
- Regression tests for the three blockers
- Updated workflow golden file and structural invariants
- Updated `06-VERIFICATION.md` and `06-09-SUMMARY.md`
