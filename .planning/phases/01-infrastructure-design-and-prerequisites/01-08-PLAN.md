---
phase: 01-infrastructure-design-and-prerequisites
plan: 08
type: execute
wave: 7
depends_on: [01-06, 01-07]
files_modified:
  - infrastructure/inventory.json
  - scripts/validate-inventory.sh
  - tests/test-inventory.sh
autonomous: false
gap_closure: true
requirements: [INFRA-03]
must_haves:
  truths:
    - "The inventory records a measured usable capacity for every target storage pool, not only commitments and a claimed percentage."
    - "The validator derives storage headroom from measured pool capacity, existing commitments, and planned allocations, and fails when the derived value breaches the 20% minimum."
    - "An inventory whose recorded resulting_headroom_percent disagrees with its measured storage inputs is rejected with a capacity diagnostic."
  artifacts:
    - path: "infrastructure/inventory.json"
      provides: "Measured total_gib per storage pool plus a resulting_headroom_percent reproducible from measured inputs"
    - path: "scripts/validate-inventory.sh"
      provides: "Derived storage-headroom gate with claim-consistency and commitment cross-checks"
    - path: "tests/test-inventory.sh"
      provides: "Rejection coverage for inconsistent claims, derived-headroom breaches, and missing measured capacity"
  key_links:
    - from: "infrastructure/inventory.json"
      to: "scripts/validate-inventory.sh"
      via: "total_gib consumed as the denominator of derived storage-headroom arithmetic"
      pattern: "total_gib"
    - from: "tests/test-inventory.sh"
      to: "scripts/validate-inventory.sh"
      via: "inline storage mutations must be rejected with capacity diagnostics"
      pattern: "storage-"
---

<objective>
Close verification Gap 1: storage headroom is currently asserted rather than calculated. Record the measured usable capacity of the `local-lvm` pool, make the validator derive storage headroom from measured capacity plus existing commitments plus planned allocations, and prove rejection of inconsistent storage claims.

Purpose: The MS-01 resource budget truth requires a reproducible storage-headroom calculation, matching how CPU ratio and memory headroom are already derived from measured values.
Output: Inventory with measured pool capacity, a derivation-based validator gate, and negative tests for storage inconsistency.
</objective>

<execution_context>
@/home/tonny/.codex/gsd-core/workflows/execute-plan.md
@/home/tonny/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-infrastructure-design-and-prerequisites/01-VERIFICATION.md
@.planning/phases/01-infrastructure-design-and-prerequisites/01-04-SUMMARY.md
@infrastructure/inventory.json
@scripts/validate-inventory.sh
@tests/test-inventory.sh
</context>

## Artifacts this phase produces

- JSON field `site.proxmox.storage_pools.value[].total_gib` (measured usable pool capacity, GiB) in `infrastructure/inventory.json`
- Recomputed JSON field `site.proxmox.storage_pools.value[].resulting_headroom_percent` (one-decimal value derived from measured inputs)
- Updated `validation` and `source` narrative strings on `site.proxmox.storage_pools` referencing the measured observation
- Validator checks in `scripts/validate-inventory.sh`: per-pool `total_gib` presence/positivity requirement, derived storage-headroom threshold check, claim-vs-derived consistency check (tolerance 0.1 percentage points), pool-commitment cross-check against `site.proxmox.existing_committed_storage_gib`, and planned-allocation cross-check against opentofu guest `disk_gib` totals
- Inline test cases in `tests/test-inventory.sh`: `storage-claim-inconsistent`, `storage-derived-breach`, `storage-missing-measured-total`
- Evidence record of the operator-supplied measurement in `.planning/phases/01-infrastructure-design-and-prerequisites/01-08-SUMMARY.md`

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator supplies the measured usable capacity of local-lvm</name>
  <files>.planning/phases/01-infrastructure-design-and-prerequisites/01-08-SUMMARY.md</files>
  <read_first>
    - infrastructure/inventory.json (current storage_pools entry: existing_committed_gib 323.15, planned_gib 192, claimed resulting_headroom_percent 69.9, no measured denominator)
    - .planning/phases/01-infrastructure-design-and-prerequisites/01-04-SUMMARY.md (evidence-recording precedent: non-secret facts keyed by inventory path with source and timestamp)
  </read_first>
  <action>Ask the operator for one read-only observation: the usable total capacity of the Proxmox storage pool `local-lvm` on node `proxmox`, in GiB (or in bytes, which the executor converts as bytes divided by 2^30). Acceptable read paths the operator may use: the Proxmox web UI storage summary, `pvesm status` on the host, or an authenticated GET of the node storage status API with the existing PVEAuditor identity. Request exactly three non-secret facts: the numeric capacity value with its unit, the observation method used, and the observation date/time. Repository automation has no credential path to Proxmox, so this value cannot be self-measured (same constraint as the Plan 01-04 checkpoint). Never request or record tokens, passwords, cookies, authorization headers, or response bodies. Sanity-check the reply before resuming: the supplied capacity must exceed 515.15 GiB (existing 323.15 plus planned 192), and the headroom it implies should be near the previously claimed 69.9 percent; if it is not, ask the operator to re-confirm the reading instead of proceeding silently.</action>
  <verify><automated>MISSING — operator-controlled Proxmox storage measurement; no repository credential path can read the pool capacity</automated></verify>
  <acceptance_criteria>
    - The operator reply contains a numeric capacity value greater than 515.15 GiB (after unit conversion if supplied in bytes), an observation method, and an observation date.
    - No secret material (token, password, cookie, header, response body) appears in the recorded evidence.
    - The evidence (value, method, date) is restated in the plan summary keyed to inventory path `site.proxmox.storage_pools`.
  </acceptance_criteria>
  <done>A sourced, dated, non-secret measured capacity for `local-lvm` is available for Task 2 to record.</done>
  <resume-signal>Reply with the measured usable capacity of local-lvm (GiB or bytes), how you read it, and when.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record measured pool capacity and reproduce the headroom figure in the inventory</name>
  <files>infrastructure/inventory.json</files>
  <read_first>
    - infrastructure/inventory.json (the storage_pools evidence object being extended; preserve its value/status/validation/source shape)
    - scripts/validate-inventory.sh (current checks the updated inventory must keep passing before Task 3 changes them)
  </read_first>
  <action>In `infrastructure/inventory.json`, extend the single `local-lvm` object inside `site.proxmox.storage_pools.value` with a new numeric field `total_gib` holding the operator-supplied measured usable capacity in GiB from Task 1 (round to two decimals if converted from bytes). Recompute `resulting_headroom_percent` for that pool as (total_gib minus existing_committed_gib minus planned_gib) times 100 divided by total_gib, rounded to one decimal, replacing the current 69.9 only if the derived value differs. Update the `validation` string of `site.proxmox.storage_pools` to state that the recorded headroom is reproduced from measured usable pool capacity together with existing and planned commitments, and extend the `source` string to name the operator-supplied Proxmox storage status observation with its date from Task 1. If the derived value also changes the approximate figure quoted in the `validation` string of `capacity.policy.minimum_storage_headroom_percent`, update that narrative to the new figure. If the derived headroom is below 20, stop and surface the contradiction to the operator instead of recording it. Do not change any other inventory field, and do not add credential-shaped keys.</action>
  <verify><automated>jq -e '.site.proxmox.storage_pools.value[0] | (.total_gib|type=="number" and .>515.15) and ((((.total_gib - .existing_committed_gib - .planned_gib) * 100 / .total_gib) - .resulting_headroom_percent) | (if .&lt;0 then -. else . end) &lt;= 0.1)' infrastructure/inventory.json &amp;&amp; bash scripts/validate-inventory.sh infrastructure/inventory.json</automated></verify>
  <acceptance_criteria>
    - `site.proxmox.storage_pools.value[0].total_gib` records the sourced usable capacity as a positive number greater than committed plus planned storage.
    - `resulting_headroom_percent` is reproducible from `total_gib`, `existing_committed_gib`, and `planned_gib` within 0.1 percentage points.
    - Storage evidence narratives identify the non-secret observation method and date without recording credentials or raw authenticated responses.
  </acceptance_criteria>
  <done>The canonical inventory contains measured storage capacity and a reproducible headroom result above the approved minimum.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce derived storage arithmetic and add rejection coverage</name>
  <files>scripts/validate-inventory.sh, tests/test-inventory.sh</files>
  <read_first>
    - scripts/validate-inventory.sh (existing capacity checks and diagnostic conventions)
    - tests/test-inventory.sh (fixture mutation helpers and expected-failure conventions)
    - infrastructure/inventory.json (new measured `total_gib` field and canonical commitment values)
  </read_first>
  <action>Replace the storage check that trusts `resulting_headroom_percent` with per-pool arithmetic derived from `total_gib`, `existing_committed_gib`, and `planned_gib`. Require all four numeric fields, positive measured capacity, non-negative commitments, and commitments not exceeding measured capacity. Reject a derived percentage below `capacity.policy.minimum_storage_headroom_percent.value`. Separately reject a recorded `resulting_headroom_percent` that differs from the derived one by more than 0.1 percentage points, using a generic storage-capacity diagnostic. Cross-check the sum of pool `existing_committed_gib` against `site.proxmox.existing_committed_storage_gib.value`, and the sum of pool `planned_gib` against the `disk_gib` total for OpenTofu-owned planned guests. Add mutation cases named `storage-claim-inconsistent`, `storage-derived-breach`, and `storage-missing-measured-total`; each must fail for the intended storage-capacity reason. Preserve all existing acceptance and rejection tests and do not print inventory values that might later become sensitive.</action>
  <verify><automated>bash tests/test-inventory.sh &amp;&amp; bash scripts/validate-inventory.sh infrastructure/inventory.json</automated></verify>
  <acceptance_criteria>
    - The validator computes storage headroom from measured capacity and commitments instead of accepting a self-reported result.
    - Missing measured capacity, inconsistent claims, and a derived reserve below 20 percent are rejected.
    - Existing and planned per-pool commitments must agree with their canonical inventory totals.
    - The complete pre-existing validator suite remains green.
  </acceptance_criteria>
  <done>Storage headroom is reproducible, policy-enforced, and protected by targeted negative tests.</done>
</task>

</tasks>

<verification>
After the operator measurement is recorded, `bash tests/test-inventory.sh` and `bash scripts/validate-inventory.sh infrastructure/inventory.json` both pass, while all three storage mutations fail with safe capacity diagnostics.
</verification>

<success_criteria>
Verification Gap 1 is closed: INFRA-03 storage headroom is derived from a sourced measured denominator, cross-checked against canonical commitments, and covered by rejection tests.
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure-design-and-prerequisites/01-08-SUMMARY.md` when done, including only the measured value, observation method, observation date, changed files, test results, and commits; never include authentication material.
</output>
