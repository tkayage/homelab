---
phase: 01-infrastructure-design-and-prerequisites
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - docs/prerequisites.md
  - docs/architecture-boundaries.md
autonomous: true
requirements: [INFRA-03]
must_haves:
  truths:
    - "The operator can collect and verify every site fact and access prerequisite without putting secrets in git."
    - "Later phases can identify the sole owner of every infrastructure capability."
  artifacts:
    - path: "docs/prerequisites.md"
      provides: "Operator acquisition and non-secret access verification checklist"
    - path: "docs/architecture-boundaries.md"
      provides: "Ownership, durability, dependency, and security boundary contract"
  key_links:
    - from: "docs/prerequisites.md"
      to: "infrastructure/inventory.json"
      via: "exact JSON paths for recording verified facts"
      pattern: "inventory.json"
    - from: "docs/architecture-boundaries.md"
      to: "infrastructure/inventory.json"
      via: "canonical owner values and system IDs"
      pattern: "argocd|opentofu|config-automation|manual-existing"
---

<objective>
Document how the operator validates the canonical contract and how platform ownership is divided.

Purpose: Make prerequisite acquisition repeatable, safe, and usable by every later phase.
Output: A concrete prerequisite checklist and an architecture/security boundary reference tied to inventory paths.
</objective>

<execution_context>
@/home/tonny/.codex/gsd-core/workflows/execute-plan.md
@/home/tonny/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-design-and-prerequisites/01-CONTEXT.md
@.planning/phases/01-infrastructure-design-and-prerequisites/01-RESEARCH.md
@.planning/phases/01-infrastructure-design-and-prerequisites/01-01-SUMMARY.md
@infrastructure/inventory.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the site-fact and access prerequisite checklist</name>
  <files>docs/prerequisites.md</files>
  <action>Write an operator checklist whose tables use exact canonical inventory JSON paths as row keys. Cover PVE version/node/bridge/storage/template identifiers; measured physical threads, usable RAM/storage, existing vCPU/RAM/storage commitments; VLAN/subnet/gateway/DNS/static range; collision checks against DHCP and Mikrotik state; NPM identity; domain/Cloudflare zone; GitHub repository; and GHCR package ownership. Include dedicated Zitadel rows for observed guest ID, IPv4, DNS name, vCPU, memory MiB, disk GiB, startup order, startup delay, evidence source, and measurement timestamp. For Proxmox, Mikrotik, NPM, Cloudflare, GitHub, and GHCR, require a structured access row with system ID, minimum scope, secret-store label, consumers, rotation procedure, revocation procedure, verification procedure, outcome, evidence source, and timestamp. Commands must reference environment-variable names or interactive authentication, never inline values. Include separate structured approval rows for guest sizes, 20% memory/storage reserves, and the CPU policy mode, numeric threshold, accounting basis, rationale, approver, and date.</action>
  <verify><automated>bash -c 'set -e; f=docs/prerequisites.md; for path in site.proxmox site.network capacity.host.physical_threads capacity.commitments.existing_vcpu capacity.policy.cpu guests.zitadel-existing.resources.vcpu guests.zitadel-existing.resources.memory_mib guests.zitadel-existing.resources.disk_gib guests.zitadel-existing.network.ipv4 guests.zitadel-existing.network.dns guests.zitadel-existing.startup.order guests.zitadel-existing.startup.delay_seconds; do rg -q "^[|][[:space:]]*${path//./\\.}[[:space:]]*[|]" "$f"; done; for system in proxmox mikrotik-lan npm cloudflare github ghcr; do rg -q "^[|][[:space:]]*$system[[:space:]]*[|]" "$f"; done; rg -q "minimum scope.*secret-store.*consumers.*rotation.*revocation.*verification.*outcome.*source.*timestamp" "$f"'</automated></verify>
  <done>The operator has an exhaustive, secret-safe procedure for resolving every blocked fact and confirming every access prerequisite.</done>
</task>

<task type="auto">
  <name>Task 2: Define architecture, lifecycle, and security ownership</name>
  <files>docs/architecture-boundaries.md</files>
  <action>Document the approved topology and a structured responsibility table with one row per required system/capability and columns for canonical inventory ID/path, lifecycle owner enum, configuration owner enum, state class, and trust boundary. Rows must cover k3s, Postgres, Valkey, NATS, Debezium, Zitadel, Kubernetes desired state, VM/LXC lifecycle, native-service configuration, local exposure, and public opt-in. Use only canonical owner values `argocd`, `opentofu`, `config-automation`, and `manual-existing` and link mutable facts to inventory paths rather than copying IPs or resource sizes. Explain that Proxmox startup order handles boot sequencing while later readiness checks handle service availability, and no Phase 1 action provisions or mutates infrastructure.</action>
  <verify><automated>bash -c 'set -e; f=docs/architecture-boundaries.md; for id in k3s-01 postgres-01 valkey-01 nats-01 debezium-01 zitadel-existing kubernetes-desired-state vm-lxc-lifecycle native-service-configuration local-exposure public-opt-in; do rg -q "^[|][[:space:]]*$id[[:space:]]*[|]" "$f"; done; rg -q "canonical inventory.*lifecycle owner.*configuration owner.*state class.*trust boundary" "$f"; ! rg -n "^[|].*([0-9]{1,3}\\.){3}[0-9]{1,3}.*[|]$|^[|].*[0-9]+[[:space:]]*(MiB|GiB|vCPU).*[|]$" "$f"'</automated></verify>
  <done>Every capability has one documented owner and the security/durability boundaries match the approved architecture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External service dashboards → operator | Access checks may expose sensitive values if procedures are unsafe. |
| Documentation → later executors | Ambiguous ownership can cause destructive double-management. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Information Disclosure | prerequisite commands | critical | mitigate | Use variable names/redacted probes and separate secret-store instructions. |
| T-01-08 | Tampering | ownership boundary | high | mitigate | Assign one owner per capability and bind mutable facts to inventory paths. |
| T-01-09 | Elevation of Privilege | external access prerequisites | high | mitigate | Specify least privilege, consumers, rotation, and revocation per system. |
</threat_model>

<verification>
Documentation coverage commands pass and no literal credential material is introduced; mutable addresses and allocations remain canonical in JSON.
</verification>

<success_criteria>
An operator can safely acquire all missing inputs and later executors can determine ownership without architecture ambiguity.
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure-design-and-prerequisites/01-03-SUMMARY.md` when done.
</output>
