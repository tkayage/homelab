---
phase: 01-infrastructure-design-and-prerequisites
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infrastructure/inventory.json
autonomous: true
requirements: [INFRA-03]
must_haves:
  truths:
    - "Every planned VM and native-service LXC has an explicit proposed CPU, memory, disk, network identity, and startup order."
    - "CPU commitments preserve an explicit operator-approved reserve or overcommit limit against measured host capacity."
    - "Unknown site facts are visibly blocked rather than represented as plausible production values."
    - "Credential requirements describe names and scopes without containing credential material."
  artifacts:
    - path: "infrastructure/inventory.json"
      provides: "Canonical machine-readable infrastructure contract"
      contains: '"guests"'
  key_links:
    - from: "infrastructure/inventory.json"
      to: "Phase 2 and Phase 5 provisioning"
      via: "stable guest IDs, owners, resources, network identities, dependencies, and startup metadata"
      pattern: '"owner"|"depends_on"|"startup"'
---

<objective>
Create the canonical, secret-free infrastructure inventory while preserving the approved architecture and explicit evidence states.

Purpose: Establish the single source of truth that all later provisioning and configuration phases consume.
Output: `infrastructure/inventory.json` with host, network, guest, ownership, capacity-policy, and credential-descriptor contracts.
</objective>

<execution_context>
@/home/tonny/.codex/gsd-core/workflows/execute-plan.md
@/home/tonny/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-design-and-prerequisites/01-CONTEXT.md
@.planning/phases/01-infrastructure-design-and-prerequisites/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define host, network, and capacity contracts</name>
  <files>infrastructure/inventory.json</files>
  <action>Create valid RFC 8259 JSON with a schema version and environment identifier. Represent each environment-specific fact as an object containing `value`, `status` (`proposed`, `verified`, or `blocked`), `validation`, and `source`; use null for unavailable values, not realistic examples. Include Proxmox version/node, physical cores/threads, usable memory, storage pools and usable/free space, existing committed vCPU/memory/storage allocations, bridge, VLAN, subnet, gateway, DNS suffix, DHCP exclusion/static range, NPM identity, and measurement timestamp. Define an explicit capacity policy with one operator-selectable CPU mode: either a minimum uncommitted physical-thread percentage or a maximum aggregate-vCPU-to-thread overcommit ratio, plus at least 20% unallocated usable RAM and datastore space. Store the selected CPU mode, numeric threshold, accounting basis, approval status, and rationale as evidence-bearing fields marked proposed until operator approval. Preserve the single-MS-01 topology and local-first boundary from CONTEXT.md.</action>
  <verify><automated>jq -e '.schema_version and .site.proxmox and .site.network and .capacity.policy and (.capacity.policy.cpu.mode.value | IN("minimum_uncommitted_thread_percent","maximum_vcpu_overcommit_ratio")) and (.capacity.policy.cpu.threshold.value | numbers) and (.capacity.policy.minimum_memory_headroom_percent.value == 20) and (.capacity.policy.minimum_storage_headroom_percent.value == 20)' infrastructure/inventory.json >/dev/null</automated></verify>
  <done>The canonical inventory parses and every unknown site fact has explicit blocked evidence metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Inventory guests, ownership, dependencies, and credential descriptors</name>
  <files>infrastructure/inventory.json</files>
  <action>Add exactly one disposable `k3s-01` VM and native-service LXCs for Postgres, Valkey, NATS JetStream, and Debezium; retain Zitadel as `manual-existing` without resizing it. Record proposed allocations from research: k3s 4 vCPU/8192 MiB/64 GiB; Postgres 2/4096/64; Valkey 1/2048/16; NATS 1/2048/32; Debezium 2/4096/16. Give every guest a blocked stable IPv4 and DNS identity, owner, durable/ephemeral classification, `depends_on`, startup order and delay; use orders 10 Postgres, 20 Valkey/NATS, 30 Debezium, 40 k3s. For `zitadel-existing`, define blocked evidence-bearing fields for observed guest ID, IPv4, DNS name, vCPU, memory MiB, disk GiB, startup order, startup delay, measurement source, and measurement timestamp; these record current capacity and identity only and never authorize resizing. Define the responsibility map so Argo CD owns Kubernetes resources only, OpenTofu owns guest/external-infrastructure lifecycle, configuration automation owns native service configuration, and existing systems are manual-existing. Add descriptors for Proxmox, Mikrotik LAN, NPM, Cloudflare, GitHub, and GHCR access with logical name, owner, minimum scope, storage location, consumers, rotation/revocation notes, and non-secret acquisition/verification guidance. Do not add fields designed to hold secret values or perform any deferred provisioning, credential creation, router, or external-service mutation.</action>
  <verify><automated>jq -e '([.guests[].id] | sort) == (["debezium-01","k3s-01","nats-01","postgres-01","valkey-01","zitadel-existing"] | sort) and (all(.guests[]; has("resources") and has("network") and has("startup"))) and ((.guests[] | select(.id == "zitadel-existing")) as $z | $z.owner == "manual-existing" and ($z | has("observed_guest_id")) and ($z.resources | has("vcpu") and has("memory_mib") and has("disk_gib")) and ($z.network | has("ipv4") and has("dns")) and ($z.startup | has("order") and has("delay_seconds")) and ($z | has("measurement_source") and has("measurement_timestamp"))) and ([.credentials[].system] | sort) == (["cloudflare","github","ghcr","mikrotik-lan","npm","proxmox"] | sort)' infrastructure/inventory.json >/dev/null</automated></verify>
  <done>All platform guests and access prerequisites are explicit, secret-free, and assigned to the approved ownership boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository → later automation | Inventory values will drive infrastructure mutations in later phases. |
| Operator secret store → repository | Only credential descriptors may cross into version control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | inventory identities and allocations | high | mitigate | Attach evidence state/source to every site fact and validate before consumption. |
| T-01-02 | Information Disclosure | credential descriptors | critical | mitigate | Model names/scopes only; prohibit secret-value fields and plaintext material. |
| T-01-03 | Elevation of Privilege | credential scopes | high | mitigate | Record minimum scopes and separate credentials by target system. |
</threat_model>

<verification>
`jq empty infrastructure/inventory.json` succeeds and inventory queries prove complete allocations, identities, ownership, and access descriptors.
</verification>

<success_criteria>
The repository has one canonical contract containing every INFRA-03 allocation and every Phase 1 architecture/access boundary without presenting unknown values as verified facts.
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure-design-and-prerequisites/01-01-SUMMARY.md` when done.
</output>
