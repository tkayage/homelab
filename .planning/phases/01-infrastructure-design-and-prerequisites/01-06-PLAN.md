---
phase: 01-infrastructure-design-and-prerequisites
plan: 06
type: execute
wave: 5
depends_on: [01-05]
files_modified:
  - scripts/validate-inventory.sh
  - tests/test-inventory.sh
  - tests/fixtures/invalid-capacity.json
  - tests/fixtures/invalid-duplicate-ip.json
  - tests/fixtures/invalid-secret-field.json
  - tests/fixtures/invalid-unresolved.json
autonomous: true
requirements: [INFRA-03]
must_haves:
  truths:
    - "The validator accepts exactly the approved three-new-guest topology and rejects obsolete per-service guests."
    - "The approved 20 GiB memory allocation, 2.5 CPU ratio, and 20% RAM/storage reserves pass measured capacity checks."
  artifacts:
    - path: "scripts/validate-inventory.sh"
      provides: "Policy gate for the consolidated Compose VM topology"
    - path: "tests/test-inventory.sh"
      provides: "Acceptance and rejection coverage for the consolidated topology"
  key_links:
    - from: "infrastructure/inventory.json"
      to: "scripts/validate-inventory.sh"
      via: "validator asserts exact guest topology and embedded Compose services"
      pattern: "services-01"
---

<objective>
Update automated policy enforcement against the consolidated-services architecture.

Purpose: Prove the migrated topology is enforced before the final Phase 1 gate runs.
Output: Topology-aware validator and acceptance/rejection tests ready for Plan 01-07.
</objective>

<execution_context>
@/home/tonny/.codex/gsd-core/workflows/execute-plan.md
@/home/tonny/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure-design-and-prerequisites/01-04-SUMMARY.md
@.planning/phases/01-infrastructure-design-and-prerequisites/01-05-SUMMARY.md
@infrastructure/inventory.json
@scripts/validate-inventory.sh
@tests/test-inventory.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update topology validation and fixtures</name>
  <files>scripts/validate-inventory.sh, tests/test-inventory.sh, tests/fixtures/invalid-capacity.json, tests/fixtures/invalid-duplicate-ip.json, tests/fixtures/invalid-secret-field.json, tests/fixtures/invalid-unresolved.json</files>
  <behavior>
    - A resolved inventory with postgres-01 LXC, services-01 VM containing Valkey/NATS/Debezium, and k3s-01 VM is accepted.
    - A legacy per-service guest, missing Compose service, Docker-host LXC, wrong fixed identity/allocation, broken dependency, unresolved evidence, duplicate address, secret-shaped field, or breached capacity policy is rejected.
    - Compose service dependencies/readiness are validated inside services-01 without assigning Proxmox startup orders to embedded services.
  </behavior>
  <action>First update the test builder and fixtures to the consolidated topology and add rejection cases for legacy per-service guests, a missing required Compose service, and any Docker Compose host whose kind is not `vm`; confirm those tests fail against the old validator. Then change fixed-topology rules to require exactly `postgres-01` LXC, `services-01` VM, and `k3s-01` VM among new OpenTofu guests, plus observe-only `zitadel-existing`. Enforce exact approved VMIDs, identities, allocations, startup values, the 20480 MiB aggregate, k3s dependencies, internal service set, Compose-level readiness/dependency metadata, and Docker-in-LXC prohibition. Retain evidence, uniqueness, ownership, secret-safety, Zitadel completeness, CPU ratio, RAM reserve, and datastore reserve checks. Remove assertions that embedded services are Proxmox guests and keep every invalid fixture focused on its named failure.</action>
  <verify><automated>bash -n scripts/validate-inventory.sh && bash tests/test-inventory.sh</automated></verify>
  <done>The suite accepts the approved consolidated topology and rejects obsolete topology plus all relevant policy violations.</done>
</task>

</tasks>

<verification>
The updated suite and canonical validator pass against the consolidated topology.
</verification>

<success_criteria>
Plan 01-07 can run the complete Phase 1 gate using topology-aware policy enforcement.
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure-design-and-prerequisites/01-06-SUMMARY.md` when done.
</output>
