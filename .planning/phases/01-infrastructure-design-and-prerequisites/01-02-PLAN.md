---
phase: 01-infrastructure-design-and-prerequisites
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - scripts/validate-inventory.sh
  - tests/fixtures/invalid-duplicate-ip.json
  - tests/fixtures/invalid-secret-field.json
  - tests/fixtures/invalid-capacity.json
  - tests/fixtures/invalid-unresolved.json
  - tests/test-inventory.sh
autonomous: true
requirements: [INFRA-03]
must_haves:
  truths:
    - "Unsafe, contradictory, over-capacity, or unresolved inventories fail with actionable errors."
    - "A fully verified inventory within approved headroom passes without extra dependencies beyond Bash and jq."
  artifacts:
    - path: "scripts/validate-inventory.sh"
      provides: "Structural, identity, secret-shape, evidence-state, and capacity policy gate"
    - path: "tests/test-inventory.sh"
      provides: "Executable acceptance and rejection coverage"
  key_links:
    - from: "scripts/validate-inventory.sh"
      to: "infrastructure/inventory.json"
      via: "jq structural and arithmetic checks"
      pattern: "jq"
    - from: "tests/test-inventory.sh"
      to: "scripts/validate-inventory.sh"
      via: "positive and negative fixture execution"
      pattern: "validate-inventory"
---

<objective>
Build a dependency-free enforcement gate for the infrastructure contract.

Purpose: Prevent unresolved placeholders, collisions, secret leakage, and resource overcommit from reaching provisioning phases.
Output: Bash/jq validator, focused invalid fixtures, and a self-contained test runner.
</objective>

<execution_context>
@/home/tonny/.codex/gsd-core/workflows/execute-plan.md
@/home/tonny/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-design-and-prerequisites/01-RESEARCH.md
@.planning/phases/01-infrastructure-design-and-prerequisites/01-01-SUMMARY.md
@infrastructure/inventory.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement structural, identity, and secret-safety validation</name>
  <files>scripts/validate-inventory.sh</files>
  <action>Create an executable Bash script with `set -euo pipefail`, optional inventory path argument, dependency checks, and accumulated actionable failures. Use jq for JSON parsing and policies. Require complete guest resource/network/startup/owner/dependency shapes; unique guest IDs, non-null IPv4s, and DNS names for the final gate; valid owner and evidence-state enums; resolvable dependency IDs; and no blocked/proposed required site facts. Reject example suffixes, placeholder strings, PEM markers, authorization-header material, long opaque secret-like strings, and case-insensitive secret-shaped object keys anywhere in inventory. Keep diagnostics field-specific and never print values from credential-like locations.</action>
  <verify><automated>bash -n scripts/validate-inventory.sh && test -x scripts/validate-inventory.sh</automated></verify>
  <done>The validator rejects incomplete or unsafe contracts and reports each failed policy without leaking suspect content.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce capacity and architecture boundaries</name>
  <files>scripts/validate-inventory.sh</files>
  <action>Extend the validator to sum existing committed and proposed vCPU, memory, and storage allocations and compare them with measured usable host resources. Enforce the approved CPU policy according to its selected mode: calculate either uncommitted physical-thread percentage or aggregate-vCPU-to-thread ratio and reject a breached threshold; also enforce approved memory/datastore headroom percentages. Require approval evidence, measurement timestamp, and source for the policy and capacity inputs. Assert exactly one disposable k3s VM; native-service LXCs for Postgres, Valkey, NATS, and Debezium; retained manual-existing Zitadel with verified observed guest ID, IP, DNS, vCPU, memory, disk, startup order/delay, source, and timestamp; unique startup orders where dependency sequencing requires them; and the approved ownership boundary that prevents Argo CD from owning external infrastructure. Validate credential descriptor completeness and minimum-scope text while prohibiting any credential value container.</action>
  <verify><automated>bash -n scripts/validate-inventory.sh && rg -q 'minimum_uncommitted_thread_percent|maximum_vcpu_overcommit_ratio' scripts/validate-inventory.sh && rg -q 'zitadel-existing' scripts/validate-inventory.sh && rg -q 'argocd|Argo' scripts/validate-inventory.sh</automated></verify>
  <done>Capacity headroom and the platform's topology/ownership invariants are machine-enforced.</done>
</task>

<task type="auto">
  <name>Task 3: Prove acceptance and rejection paths</name>
  <files>tests/fixtures/invalid-duplicate-ip.json, tests/fixtures/invalid-secret-field.json, tests/fixtures/invalid-capacity.json, tests/fixtures/invalid-unresolved.json, tests/test-inventory.sh</files>
  <action>Create compact fixtures derived from the canonical shape that independently violate IP uniqueness, secret safety, resource headroom, and evidence completeness. Make the capacity fixture exercise the selected CPU policy as well as RAM/storage arithmetic, and make unresolved coverage prove a missing Zitadel observed field is rejected. Build an executable test runner that creates a temporary verified valid inventory from the canonical contract without changing the repository file, asserts it passes, asserts every invalid fixture fails, checks relevant diagnostic categories, and cleans temporary data with a trap. Ensure tests never echo possible secret content. Do not install a test framework.</action>
  <verify><automated>bash tests/test-inventory.sh</automated></verify>
  <done>The test runner proves the validator accepts a safe resolved contract and rejects each high-risk failure mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inventory input → validator | Repository content is untrusted input to a future provisioning gate. |
| Validator diagnostics → logs | Errors must identify fields without disclosing credential-like values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | validator input | high | mitigate | Strict jq shape, enum, uniqueness, and ownership checks. |
| T-01-05 | Denial of Service | host capacity | high | mitigate | Arithmetic gates preserve approved RAM and datastore headroom. |
| T-01-06 | Information Disclosure | validation diagnostics | high | mitigate | Emit field paths/categories, never suspect values. |
</threat_model>

<verification>
`bash tests/test-inventory.sh` passes in under 60 seconds using only installed Bash and jq.
</verification>

<success_criteria>
The contract has an automated gate covering structure, resolution state, uniqueness, secrets, capacity, topology, startup dependencies, and ownership.
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure-design-and-prerequisites/01-02-SUMMARY.md` when done.
</output>
