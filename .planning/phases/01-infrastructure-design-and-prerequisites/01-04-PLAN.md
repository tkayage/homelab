---
phase: 01-infrastructure-design-and-prerequisites
plan: 04
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified: []
autonomous: false
requirements: [INFRA-03]
must_haves:
  truths:
    - "The operator supplies measured site facts and explicitly accepts or adjusts proposed budgets, including a measurable CPU policy, before provisioning."
    - "The operator supplies complete source-dated observed capacity, identity, and startup evidence for existing Zitadel."
    - "No credential value is requested or transmitted through the checkpoint."
  artifacts:
    - path: ".planning/phases/01-infrastructure-design-and-prerequisites/01-04-SUMMARY.md"
      provides: "Structured operator decisions and verified non-secret facts for finalization"
  key_links:
    - from: "docs/prerequisites.md"
      to: "01-04-SUMMARY.md"
      via: "operator reports each checklist result by inventory path"
      pattern: "site|capacity|network|access"
---

<objective>
Collect the environment-specific facts and budget approvals that cannot be discovered from the repository.

Purpose: Replace assumptions with operator-verified input before the contract is allowed to pass its final gate.
Output: A structured checkpoint summary containing non-secret site facts, approvals, and access-check outcomes.
</objective>

<execution_context>
@/home/tonny/.codex/gsd-core/workflows/execute-plan.md
@/home/tonny/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-design-and-prerequisites/01-02-SUMMARY.md
@.planning/phases/01-infrastructure-design-and-prerequisites/01-03-SUMMARY.md
@infrastructure/inventory.json
@docs/prerequisites.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provide verified site facts and prerequisite outcomes</name>
  <action>Follow `docs/prerequisites.md` using read-only inspection and report each result by its exact inventory path. Return non-secret values for Proxmox version/node, physical CPU threads, usable memory, each storage pool capacity/free space, existing committed vCPU/memory/storage allocations, bridge/VLAN/subnet/gateway/DNS suffix/static range, selected unique VM/LXC IDs and IP/DNS names, and NPM identity with source and measurement timestamp. For existing Zitadel, explicitly return observed guest ID, IPv4, DNS name, vCPU, memory MiB, disk GiB, startup order, startup delay, evidence source, and measurement timestamp. Accept or replace proposed guest sizes and startup ordering; approve one CPU policy mode with its numeric reserve/overcommit threshold, accounting basis, rationale, approver/date; and approve or replace the 20% RAM/datastore reserves. For each external system, report access verification outcome, evidence source/timestamp, and credential storage location label only. Never paste passwords, tokens, private keys, cookies, authorization headers, or recovery codes.</action>
  <verify><automated>MISSING — this checkpoint supplies environment facts that repository automation cannot independently observe</automated></verify>
  <done>All required inventory fields have a supplied value/evidence source, resource policies are explicitly approved, and each access check has a non-secret outcome.</done>
  <resume-signal>Reply with the completed prerequisite checklist values and approvals, or identify any item that remains blocked.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator environment → execution transcript | Site facts are allowed; authentication material is prohibited. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-10 | Information Disclosure | checkpoint response | critical | mitigate | Explicitly request only non-secret facts and storage labels. |
| T-01-11 | Spoofing | reported environment facts | high | mitigate | Require source/measurement timestamp and access-check outcome for every fact group. |
</threat_model>

<verification>
Human checkpoint response covers every required checklist group without containing authentication material.
</verification>

<success_criteria>
The next plan has sufficient verified, non-secret input to finalize and validate the inventory without guessing.
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure-design-and-prerequisites/01-04-SUMMARY.md` when done, recording the supplied non-secret facts and decisions.
</output>
