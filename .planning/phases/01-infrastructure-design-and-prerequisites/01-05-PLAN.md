---
phase: 01-infrastructure-design-and-prerequisites
plan: 05
type: execute
wave: 4
depends_on: [01-04]
files_modified:
  - infrastructure/inventory.json
  - docs/prerequisites.md
autonomous: true
requirements: [INFRA-03]
must_haves:
  truths:
    - "The canonical inventory contains verified explicit CPU, memory, disk, IP/DNS, and startup allocations for every planned guest."
    - "Measured aggregate capacity retains the operator-approved CPU, memory, and storage headroom or overcommit limits."
    - "Existing Zitadel has complete verified capacity, identity, startup, source, and timestamp evidence without being resized."
    - "Every access prerequisite is documented as verified without plaintext secrets."
    - "The complete Phase 1 contract passes its automated policy gate."
  artifacts:
    - path: "infrastructure/inventory.json"
      provides: "Resolved and validated infrastructure contract"
    - path: "docs/prerequisites.md"
      provides: "Completed non-secret prerequisite sign-off"
  key_links:
    - from: "01-04-SUMMARY.md"
      to: "infrastructure/inventory.json"
      via: "operator-supplied facts replace blocked fields with verified evidence"
      pattern: '"status": "verified"'
    - from: "infrastructure/inventory.json"
      to: "scripts/validate-inventory.sh"
      via: "final automated phase gate"
      pattern: "validate-inventory.sh"
---

<objective>
Finalize and validate the executable infrastructure contract from operator-verified facts.

Purpose: Deliver the hard, secret-safe input gate required before any infrastructure provisioning starts.
Output: Resolved inventory and completed prerequisite sign-off with passing validation.
</objective>

<execution_context>
@/home/tonny/.codex/gsd-core/workflows/execute-plan.md
@/home/tonny/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-design-and-prerequisites/01-04-SUMMARY.md
@infrastructure/inventory.json
@docs/prerequisites.md
@scripts/validate-inventory.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply verified site facts and operator decisions</name>
  <files>infrastructure/inventory.json, docs/prerequisites.md</files>
  <action>Transfer every non-secret fact and decision from `01-04-SUMMARY.md` into the matching canonical inventory path. Set evidence state to verified only when the summary includes its source/check; preserve unresolved items as blocked and stop rather than inventing values. Apply approved or adjusted resource sizes, IDs, stable IP/DNS identities, startup ordering, measured capacities/commitments, CPU policy mode and numeric threshold, RAM/storage reserve thresholds, approval rationale/date, and credential storage labels/access outcomes. Require all observed Zitadel guest ID, IP, DNS, vCPU, memory, disk, startup order/delay, source, and timestamp fields to be resolved while preserving `manual-existing` ownership and making no resizing change. Update `docs/prerequisites.md` with checkmarks, dates, and references to inventory paths without duplicating mutable values or including credentials. Do not provision guests, create credentials, or mutate Proxmox/router/NPM/Cloudflare/GitHub resources.</action>
  <verify><automated>jq -e 'all(.. | objects | select(has("status") and has("validation")); .status == "verified")' infrastructure/inventory.json >/dev/null</automated></verify>
  <done>Every required contract fact is operator-verified and the documentation records a complete secret-free sign-off.</done>
</task>

<task type="auto">
  <name>Task 2: Run the complete Phase 1 gate</name>
  <files>infrastructure/inventory.json, docs/prerequisites.md</files>
  <action>Run the inventory test suite and validator against the resolved canonical file. Correct only representation, arithmetic, or checklist inconsistencies supported by the checkpoint evidence; do not weaken validation rules or convert missing input into assumptions. Confirm unique identities, complete explicit allocations, dependency/startup validity, Zitadel observed-field completeness, CPU policy approval and arithmetic, RAM/storage headroom, ownership boundaries, credential descriptor safety, and prerequisite sign-off. If any required fact remains blocked or any approved CPU/RAM/storage threshold is breached, report that exact prerequisite and leave the phase incomplete.</action>
  <verify><automated>bash tests/test-inventory.sh && scripts/validate-inventory.sh infrastructure/inventory.json && ! rg -n 'BEGIN (RSA |EC |OPENSSH )?PRIVATE KEY|Authorization:[[:space:]]*(Bearer|Basic)' infrastructure docs</automated></verify>
  <done>The canonical contract passes all automated checks and is safe for Phase 2 and Phase 5 consumption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Checkpoint summary → canonical inventory | Human-reported facts become machine-consumed provisioning inputs. |
| Canonical inventory → later provisioning | A passing contract authorizes later mutation plans to proceed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-12 | Tampering | fact transfer | high | mitigate | Require evidence in checkpoint summary and exact inventory-path mapping. |
| T-01-13 | Denial of Service | final resource budget | high | mitigate | Recalculate totals and enforce approved headroom with validator. |
| T-01-14 | Information Disclosure | final repository scan | critical | mitigate | Validator and targeted secret-material scan must pass before completion. |
</threat_model>

<verification>
`bash tests/test-inventory.sh` and `scripts/validate-inventory.sh infrastructure/inventory.json` both pass; required site facts are verified and no secret material is detected.
</verification>

<success_criteria>
INFRA-03 is fully satisfied: every guest allocation and identity is explicit, aggregate capacity retains approved headroom, access prerequisites are verified safely, and platform ownership is unambiguous.
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure-design-and-prerequisites/01-05-SUMMARY.md` when done.
</output>
