---
phase: 01-infrastructure-design-and-prerequisites
plan: 05
type: execute
wave: 4
depends_on: [01-04]
files_modified:
  - infrastructure/inventory.json
  - scripts/validate-inventory.sh
  - tests/test-inventory.sh
  - tests/fixtures/invalid-capacity.json
  - tests/fixtures/invalid-duplicate-ip.json
  - tests/fixtures/invalid-secret-field.json
  - tests/fixtures/invalid-unresolved.json
  - docs/prerequisites.md
  - docs/architecture-boundaries.md
  - .planning/phases/01-infrastructure-design-and-prerequisites/01-VALIDATION.md
autonomous: true
requirements: [INFRA-03]
must_haves:
  truths:
    - "The contract defines exactly three new guests: PostgreSQL LXC, supporting-services Compose VM, and k3s VM."
    - "Valkey, NATS/JetStream, and Debezium are services within services-01, not Proxmox guests."
    - "Docker-in-LXC remains prohibited and Compose-internal readiness is not modeled as Proxmox startup order."
    - "The approved 20 GiB planned memory, 2.5 CPU ratio, and 20% RAM/storage reserves pass measured capacity checks."
    - "Existing Zitadel remains complete, verified, observe-only, and unresized."
    - "The complete Phase 1 contract passes its updated automated policy gate."
  artifacts:
    - path: "infrastructure/inventory.json"
      provides: "Resolved three-guest infrastructure contract"
    - path: "scripts/validate-inventory.sh"
      provides: "Policy gate for the consolidated Compose VM topology"
    - path: "tests/test-inventory.sh"
      provides: "Acceptance and rejection coverage for the consolidated topology"
    - path: "docs/architecture-boundaries.md"
      provides: "Current guest and service ownership boundaries"
  key_links:
    - from: "01-04-SUMMARY.md"
      to: "infrastructure/inventory.json"
      via: "evidenced facts and exact approved assignments replace proposed or blocked leaves"
      pattern: '"status": "verified"'
    - from: "infrastructure/inventory.json"
      to: "scripts/validate-inventory.sh"
      via: "validator asserts exact guest topology and embedded Compose services"
      pattern: "services-01"
---

<objective>
Migrate, finalize, and validate the executable infrastructure contract for the approved consolidated-services architecture.

Purpose: Remove obsolete per-service guest assumptions from every machine and human contract before provisioning begins.
Output: Resolved inventory, topology-aware validator/tests, current prerequisite and ownership docs, and passing Phase 1 validation.
</objective>

<execution_context>
@/home/tonny/.codex/gsd-core/workflows/execute-plan.md
@/home/tonny/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure-design-and-prerequisites/01-04-SUMMARY.md
@infrastructure/inventory.json
@scripts/validate-inventory.sh
@tests/test-inventory.sh
@docs/prerequisites.md
@docs/architecture-boundaries.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate the canonical contract and documentation</name>
  <files>infrastructure/inventory.json, docs/prerequisites.md, docs/architecture-boundaries.md</files>
  <action>Replace the obsolete Valkey, NATS, and Debezium guest records with one `services-01` VM record. Preserve `postgres-01` as a native-service LXC and `k3s-01` as a disposable VM. Apply the exact VMID, IP, DNS, CPU, memory, disk, startup order, and delay assignments from `01-04-SUMMARY.md`; the three planned guests total 10 vCPU, 20480 MiB, and 192 GiB. Model Valkey, NATS with JetStream, and Debezium as an explicit Compose service list owned/configured inside `services-01`, including their durable-data classifications and readiness/dependency contract. State that Compose controls service order and health checks inside the VM and that Docker-in-LXC is prohibited. Make k3s depend on `postgres-01`, `services-01`, and `zitadel-existing`, not embedded service names. Transfer only evidenced site, capacity, access, and existing-Zitadel facts from the checkpoint; keep `manual-existing` ownership and do not authorize a Zitadel resize. Update prerequisites and architecture boundaries to reference `services-01`, its internal services, `postgres-01`, and `k3s-01`; retain existing Zitadel, NPM, Cloudflared, and Frigate as separate existing LXCs, and remove all claims that embedded services are separate guests. Do not provision or mutate infrastructure.</action>
  <verify><automated>jq -e '([.guests[] | select(.owner == "opentofu") | .id] | sort) == (["k3s-01","postgres-01","services-01"] | sort) and ([.guests[] | select(.id == "services-01") | .services[].id] | sort) == (["debezium","nats","valkey"] | sort) and ([.guests[] | select(.owner == "opentofu") | .resources.memory_mib.value] | add) == 20480' infrastructure/inventory.json >/dev/null && rg -q 'services-01' docs/prerequisites.md docs/architecture-boundaries.md</automated></verify>
  <done>The canonical inventory and operator docs consistently describe only the approved three new guests and the internal Compose services.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update topology validation and fixtures</name>
  <files>scripts/validate-inventory.sh, tests/test-inventory.sh, tests/fixtures/invalid-capacity.json, tests/fixtures/invalid-duplicate-ip.json, tests/fixtures/invalid-secret-field.json, tests/fixtures/invalid-unresolved.json</files>
  <behavior>
    - A resolved inventory with postgres-01 LXC, services-01 VM containing Valkey/NATS/Debezium, and k3s-01 VM is accepted.
    - A legacy per-service guest, missing Compose service, Docker-host LXC, wrong fixed identity/allocation, broken dependency, unresolved evidence, duplicate address, secret-shaped field, or breached capacity policy is rejected.
    - Compose service dependencies/readiness are validated inside services-01 without assigning Proxmox startup orders to embedded services.
  </behavior>
  <action>First update the test builder and fixtures to the consolidated topology and add rejection cases for legacy per-service guests, a missing required Compose service, and any Docker Compose host whose kind is not `vm`; confirm those tests fail against the old validator. Then change the validator's fixed-topology rules to require exactly `postgres-01` LXC, `services-01` VM, and `k3s-01` VM among new OpenTofu guests, plus observe-only `zitadel-existing`. Enforce the exact approved VMIDs, identities, allocations, startup values, the 20480 MiB aggregate, k3s dependencies, internal service set, Compose-level readiness/dependency metadata, and Docker-in-LXC prohibition. Retain general evidence, uniqueness, ownership, secret-safety, Zitadel completeness, CPU ratio, RAM reserve, and datastore reserve checks. Remove assertions that Valkey, NATS, or Debezium are Proxmox guests. Keep fixtures semantically focused so each named invalid fixture still fails for its intended reason.</action>
  <verify><automated>bash -n scripts/validate-inventory.sh && bash tests/test-inventory.sh</automated></verify>
  <done>The suite proves acceptance of the approved consolidated topology and rejects both obsolete topology and relevant policy violations.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh validation mapping and run the complete Phase 1 gate</name>
  <files>.planning/phases/01-infrastructure-design-and-prerequisites/01-VALIDATION.md, infrastructure/inventory.json, docs/prerequisites.md</files>
  <action>Update `01-VALIDATION.md` so INFRA-03, per-task mappings, Wave 0 artifacts, and manual verification describe the three new guests and the internal Compose services. Mark already executed Plan 01-01 through 01-03 rows as green historical execution while making Plan 01-05 the topology-remediation and final gate. Run the suite and validator against the resolved canonical inventory. Correct only representation, arithmetic, fixture, or checklist inconsistencies supported by `01-04-SUMMARY.md`; do not weaken a policy or invent missing evidence. Confirm all required evidence leaves are verified, the approved 2.5 vCPU/thread ratio and 20% RAM/storage reserves pass, and repository infrastructure/docs contain no private-key or authorization-header material. If evidence is missing or a threshold is breached, report the exact prerequisite and leave the phase incomplete.</action>
  <verify><automated>bash tests/test-inventory.sh && scripts/validate-inventory.sh infrastructure/inventory.json && jq -e 'all(.. | objects | select(has("status") and has("validation")); .status == "verified")' infrastructure/inventory.json >/dev/null && ! rg -n 'BEGIN (RSA |EC |OPENSSH )?PRIVATE KEY|Authorization:[[:space:]]*(Bearer|Basic)' infrastructure docs</automated></verify>
  <done>The updated canonical contract passes all automated checks and is safe for Phase 2 and Phase 5 consumption.</done>
</task>

</tasks>

<verification>
`bash tests/test-inventory.sh` and `scripts/validate-inventory.sh infrastructure/inventory.json` pass against the consolidated architecture, all evidence is resolved, and the secret-material scan is clean.
</verification>

<success_criteria>
INFRA-03 is satisfied by a verified, capacity-safe, secret-free contract containing exactly the approved PostgreSQL LXC, Compose services VM, and k3s VM topology.
</success_criteria>

<output>
Create `.planning/phases/01-infrastructure-design-and-prerequisites/01-05-SUMMARY.md` when done.
</output>
